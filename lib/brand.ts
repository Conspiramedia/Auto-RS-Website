// ============================================================
// RS AUTO — Дизайн-токены бренда. ЕДИНЫЙ ИСТОЧНИК ИСТИНЫ.
// ============================================================
// Значения перенесены из приложения (Flutter), чтобы сайт и приложение
// выглядели одинаково:
//   lib/core/theme/app_theme.dart            — seed-цвет, фон, шрифт
//   lib/shared/widgets/app_button_colors.dart — роли цветов кнопок
//
// ПРАВИЛО: цвета, радиусы, отступы и шрифты не хардкодятся в компонентах.
// Бренд дорабатывается — правка должна делаться ровно в этом файле.
// Цвета отсюда пробрасываются в Tailwind (tailwind.config.ts) и в CSS-
// переменные (app/globals.css), поэтому в разметке используются классы
// вида bg-brand-green, а не произвольные хексы.
// ============================================================

export const brand = {
  // Название площадки. Используется в <title>, OG-тегах и JSON-LD.
  name: 'RS Auto',

  colors: {
    // Основной цвет бренда (seed в Material 3 приложения).
    primary: '#1565C0',
    // Главное/подтверждающее действие: «Опубликовать», «Позвонить».
    // Это единственный акцент интерфейса — второго яркого CTA на экране быть не должно.
    green: '#22C063',
    // Связь и второстепенные действия: «Написать», «Войти».
    blue: '#1E9AF0',
    // Бренд-красный: сброс фильтров, деструктивные действия, ошибки.
    red: '#E01E23',
    // Нейтральные тёмные плашки (кнопка фильтров, шапка).
    dark: '#2B2B2E',
    // Золотой акцент: иконки на тёмном фоне, бейдж продвижения (VIP).
    gold: '#E8A73C',
    // Базовый фон страницы. Совпадает с фоном логотипа, чтобы логотип
    // не выделялся прямоугольником.
    bg: '#FFFFFF',
  },

  // Радиусы скругления. Базовые две ступени: 12 — контролы (кнопки,
  // поля, чипсы), 16 — контейнеры (карточки, модальные окна).
  radius: {
    // Мелкие бейджи поверх фотографии («Продано», «Аренда»): на плашке
    // высотой 24px радиус 12 превращает её в капсулу, а нужен именно
    // прямоугольник со скруглением.
    sm: '8px',
    control: '12px',
    card: '16px',
    // Капсула: счётчики и круглые индикаторы. Большое значение вместо
    // 9999px, чтобы одинаково работало и в CSS, и в Flutter.
    pill: '999px',
  },

  // Сетка отступов кратна 4. Все вертикальные и горизонтальные интервалы
  // берутся отсюда — это то, что делает вёрстку ритмичной.
  spacing: {
    xs: '4px',
    sm: '8px',
    md: '16px',
    lg: '24px',
    xl: '32px',
  },

  // ------------------------------------------------------------
  // Нейтральная шкала.
  // ------------------------------------------------------------
  // Значения — ровно те, что уже стояли в разметке (text-black/60,
  // border-black/10 и т.д.), переведённые в токены БЕЗ изменения цвета.
  // Это принципиально: пакет — чистый рефакторинг, сайт обязан
  // выглядеть идентично до и после.
  //
  // Почему шкала строится на прозрачном чёрном, а не на сплошных серых:
  // фон страницы белый, но карточки и плашки лежат на подложках разной
  // светлоты (bg-subtle, галерея), и полупрозрачный текст сохраняет
  // на них правильный контраст, тогда как сплошной #666 выглядел бы
  // грязным пятном.
  neutral: {
    // Текст.
    100: 'rgba(0,0,0,1)',      // основной (наследуется от body)
    80: 'rgba(0,0,0,0.8)',     // описание объявления
    75: 'rgba(0,0,0,0.75)',    // абзацы юридических документов
    70: 'rgba(0,0,0,0.7)',     // подпись согласия с условиями
    60: 'rgba(0,0,0,0.6)',     // вторичный текст — самый частый
    // Неактивная вкладка сегмента «Продажа | Аренда»: на 50 она
    // проваливается относительно активной, на 60 спорит с ней.
    55: 'rgba(0,0,0,0.55)',
    // ------------------------------------------------------------
    // ПОРОГ ЧИТАЕМОСТИ. Ступени 50 и 40 держат ФАКТИЧЕСКУЮ альфу выше
    // номера — и это не описка.
    // ------------------------------------------------------------
    // WCAG 2.1 AA требует контраста 4.5:1 для обычного текста
    // (крупным считается от 18.66px полужирного или 24px обычного —
    // у нас таким не набрана ни одна подпись). На белом фоне чёрный
    // даёт: alpha 0.40 → 2.85:1, alpha 0.50 → 3.98:1. Обе ступени
    // норму не проходили, а стоят они как раз на самом мелком тексте —
    // caption 14px (город на карточке, счётчики) и micro 11px (метки
    // dt, подписи в админке). То есть хуже всего читалось ровно то,
    // что и так набрано мельче всего.
    //
    // Правим ЗНАЧЕНИЯ, а не разметку: классы text-neutral-50 и
    // text-neutral-40 расставлены в 76 местах, и замена их на соседнюю
    // ступень означала бы 76 правок с потерей смысла шкалы —
    // «подпись» и «третьестепенное» перестали бы различаться. Номер
    // ступени здесь обозначает РОЛЬ в иерархии текста, а не альфу;
    // иерархия сохранена: 40 по-прежнему бледнее 50, 50 бледнее 55.
    //
    // Номера ступеней не меняем ещё и потому, что шкала переносится
    // во Flutter (см. шапку файла): переименование потребовало бы
    // синхронной правки темы приложения ради того же результата.
    50: 'rgba(0,0,0,0.55)',    // подписи, метки dt — 4.76:1
    // 40 подтянута к тому же порогу, а не оставлена на полступени
    // бледнее. Причина — что именно ею набрано: счётчик «(12)» внутри
    // ссылки на марку сообщает, сколько машин в разделе, и это
    // содержательный текст, а не украшение. Оставить его на 3.98:1
    // значило бы починить подписи и бросить нечитаемыми цифры,
    // ради которых по ссылке и переходят.
    //
    // Иерархия при этом держится не альфой, а размером и окружением:
    // счётчик стоит серым рядом с чёрным названием марки, «…» в
    // пагинации — между активными номерами. Разницу в полступени
    // (0.50 против 0.55) глаз на 11–14px всё равно не различал.
    40: 'rgba(0,0,0,0.55)',    // третьестепенное: счётчики, «…» — 4.76:1
    // Ступень 30 работает и как цвет текста-заглушки («нет фото»),
    // и как граница поля при наведении — значение одно и то же.
    // Поднимать её нельзя: как граница она станет заметно темнее
    // соседних, а как текст она стоит на неактивных состояниях
    // (disabled), для которых WCAG контраст не требует.
    30: 'rgba(0,0,0,0.3)',
    // Границы.
    15: 'rgba(0,0,0,0.15)',    // поля ввода, вторичные кнопки
    10: 'rgba(0,0,0,0.1)',     // карточки, разделители
  },

  // ------------------------------------------------------------
  // Нейтральная шкала для ТЁМНЫХ подложек.
  // ------------------------------------------------------------
  // Зеркало neutral: там прозрачный чёрный на белом фоне, здесь
  // прозрачный белый на тёмном. Нужна там, где текст лежит на
  // brand-dark или на цветной заливке: время и галочки в своём пузыре
  // чата, подпись и крестик смарт-баннера.
  //
  // Ступени НЕ переиспользуют номера neutral: 70 на белом фоне — это
  // почти чёрный текст, а на тёмном — приглушённый белый. Один и тот
  // же номер с противоположным смыслом путал бы при чтении разметки,
  // поэтому шкала своя и называется по подложке (text-on-dark-70).
  onDark: {
    // Вторичный текст на тёмном: время сообщения, подпись баннера.
    70: 'rgba(255,255,255,0.7)',
    // Третьестепенное: кнопка закрытия баннера.
    60: 'rgba(255,255,255,0.6)',
  },

  // Фоновые подложки и состояния наведения. Держатся отдельно от
  // neutral: это заливки, а не цвет текста, и путать их нельзя.
  surface: {
    // Секции-подложки (герой на главной, подвал).
    subtle: 'rgba(0,0,0,0.02)',
    // Плейсхолдер фотографии.
    muted: 'rgba(0,0,0,0.05)',
    // Наведение на вторичные элементы. Три ступени — ровно те,
    // что исторически стояли в разметке: чипсы сортировки чуть
    // заметнее кнопок, а активный чипс — ещё заметнее.
    hover: 'rgba(0,0,0,0.03)',
    hoverStrong: 'rgba(0,0,0,0.04)',
    hoverChip: 'rgba(0,0,0,0.05)',
    active: 'rgba(0,0,0,0.06)',
    // Наведение на уже активный (тёмный) чипс применённого фильтра.
    activeHover: 'rgba(0,0,0,0.1)',
    // Затемнение под модальным слоем: шторка фильтров и пикер на
    // мобильном. Плотность 40% — компромисс: содержимое под слоем
    // угадывается, но не отвлекает.
    overlay: 'rgba(0,0,0,0.4)',
  },

  // ------------------------------------------------------------
  // Подложки статусных сообщений.
  // ------------------------------------------------------------
  // Плашка «ошибка / успех / предупреждение» под формой и в карточке:
  // текст семантическим цветом на его же сильно разбавленной заливке.
  // Значения — ровно те, что стояли в разметке (bg-brand-red/10 и
  // bg-brand-green/10), переведённые в токены БЕЗ изменения цвета.
  //
  // Держатся отдельно от surface: те подложки нейтральные и говорят
  // об уровне поверхности, а эти несут СМЫСЛ — по цвету плашки
  // человек понимает исход действия раньше, чем читает текст.
  //
  // Плотность 10%: заливка обязана оставаться фоном под цветным
  // текстом. На 20% красный текст на красном поле начинает вибрировать,
  // на 5% плашка перестаёт читаться как выделенная область.
  statusSurface: {
    error: 'rgba(224,30,35,0.1)',   // = colors.red при 10%
    success: 'rgba(34,192,99,0.1)', // = colors.green при 10%
    warning: 'rgba(232,167,60,0.1)',// = colors.gold при 10%
    // Половинная плотность — для СТРОКИ, а не для плашки: подсветка
    // непрочитанного уведомления в ленте и наведение на «Сбросить
    // фильтры». Там заливка тянется во всю ширину, и те же 10%
    // превратились бы из подсказки в цветную полосу через весь экран.
    'error-subtle': 'rgba(224,30,35,0.05)',
    'success-subtle': 'rgba(34,192,99,0.05)',
    // Информационная подложка — тот же primary при 10%. Нужна плашке
    // «ваше устройство» на /install: она сообщает факт, а не успех и
    // не предупреждение, и зелёный с жёлтым тут читались бы как оценка.
    info: 'rgba(21,101,192,0.1)',   // = colors.primary при 10%
  },

  // ------------------------------------------------------------
  // Состояние автомобиля (миграция 0138).
  // ------------------------------------------------------------
  // ПОЧЕМУ ЭТИ ЦВЕТА НЕ ИЗ ПАЛИТРЫ БРЕНДА. Бейдж состояния — не
  // оценка действия («получилось / не получилось») и не акцент
  // интерфейса, а ПРЕДУПРЕЖДЕНИЕ О ТОВАРЕ, и различать шесть значений
  // человек должен по цвету раньше, чем прочтёт подпись. В бренде
  // ровно четыре семантических цвета, и на шесть состояний их не
  // хватает: пришлось бы красить «битую» и «без документов» одним
  // красным, а именно их и нужно различать в первую очередь.
  //
  // Набор держится ЗДЕСЬ, а не хексами в компонентах, по общему
  // правилу файла: бренд дорабатывается — правка делается в одном
  // месте. В разметку они попадают классами bg-condition-damaged и
  // bg-condition-damaged-soft через tailwind.config.ts.
  //
  // ЦВЕТ НАЗНАЧЕН ПО ТЯЖЕСТИ, а не по вкусу:
  //   damaged    — оранжевый: повреждена, но чинится;
  //   parts      — серый: уже не машина, а набор деталей;
  //   no_docs    — красный: юридический тупик, самый дорогой сюрприз
  //                для покупателя;
  //   salvage    — тёмно-серый: тот же донор, но необратимо;
  //   for_export — синий: с машиной всё в порядке, ограничение
  //                административное, и пугать цветом нечем.
  // 'normal' в наборе НЕТ намеренно: обычная машина бейджа не
  // получает, и цвет ей не нужен.
  condition: {
    damaged: '#f97316',
    parts: '#64748b',
    'no_docs': '#dc2626',
    salvage: '#475569',
    'for_export': '#2563eb',
  },

  // Подложки под пояснительную плашку на странице объявления: тот же
  // цвет при 10%, по образцу statusSurface выше. Плотность та же и по
  // той же причине — заливка обязана остаться фоном под цветным
  // текстом.
  conditionSurface: {
    damaged: 'rgba(249,115,22,0.1)',   // = condition.damaged при 10%
    parts: 'rgba(100,116,139,0.1)',    // = condition.parts при 10%
    'no_docs': 'rgba(220,38,38,0.1)',  // = condition.no_docs при 10%
    salvage: 'rgba(71,85,105,0.1)',    // = condition.salvage при 10%
    'for_export': 'rgba(37,99,235,0.1)',// = condition.for_export при 10%
  },

  // ------------------------------------------------------------
  // Семантические роли цвета.
  // ------------------------------------------------------------
  // Ссылаются на брендовые цвета, а не переопределяют их: «успех» и
  // «главное действие» — это один и тот же зелёный, и разъехаться они
  // не должны. Смысл слоя в том, чтобы в разметке читалось намерение
  // (bg-success), а не оттенок (bg-brand-green).
  semantic: {
    success: '#22C063', // = colors.green
    warning: '#E8A73C', // = colors.gold
    error: '#E01E23',   // = colors.red
    info: '#1E9AF0',    // = colors.blue
  },

  // ------------------------------------------------------------
  // Типографская шкала.
  // ------------------------------------------------------------
  // Пары «размер / межстрочный интервал» совпадают с дефолтами Tailwind,
  // которые уже используются в разметке, — поэтому перевод на токены
  // ничего не сдвигает визуально. Вес указан как рекомендуемый для роли.
  //
  // ЭТО РАЗДЕЛ ДЛЯ ПРИЛОЖЕНИЯ: именно по нему Flutter приводится 1:1.
  // Значения в px, чтобы переносились в TextStyle без пересчёта.
  typography: {
    // Верхние две ступени стоят ВЫШЕ h1 и живут отдельно от иерархии
    // заголовков: это не уровни документа, а крупная графика.
    //   hero    — цифра кода ошибки («404», «500»). Она не заголовок,
    //             а картинка: её задача — мгновенно объяснить характер
    //             страницы, поэтому размер выбит из шкалы заголовков.
    //   display — герой главной на десктопе: единственное место, где
    //             h1 вырастает над своей ступенью, потому что стоит
    //             на пустом поле и работает как обложка.
    hero: { size: '48px', lineHeight: '48px', weight: 700 },
    display: { size: '36px', lineHeight: '40px', weight: 700 },
    h1: { size: '30px', lineHeight: '36px', weight: 700 },
    h2: { size: '24px', lineHeight: '32px', weight: 700 },
    h3: { size: '20px', lineHeight: '28px', weight: 600 },
    // Подзаголовок секции: «Характеристики», «Условия аренды».
    h4: { size: '18px', lineHeight: '28px', weight: 600 },
    body: { size: '16px', lineHeight: '24px', weight: 400 },
    caption: { size: '14px', lineHeight: '20px', weight: 400 },
    small: { size: '12px', lineHeight: '16px', weight: 400 },
    // Нижняя ступень шкалы: бейджи поверх фотографии («Аренда»,
    // «Просмотрено», значок продвижения). Меньше 11px не опускаемся —
    // на мобильном такой текст перестаёт читаться, а плашки поверх
    // снимка и так стоят на пёстром фоне.
    // Отдельная ступень, а не text-[11px] в разметке: правило проекта —
    // размеры живут в токенах, иначе следующий бейдж получит «примерно
    // такой же» размер и шкала расползётся.
    micro: { size: '11px', lineHeight: '14px', weight: 400 },
  },

  // ------------------------------------------------------------
  // ЧАТ: палитра переписки.
  // ------------------------------------------------------------
  // Экран диалога — единственное место сайта, где цвет несёт не роль
  // действия, а ПРИНАДЛЕЖНОСТЬ реплики: свой пузырь против чужого.
  // Поэтому у него своя группа токенов, а не переиспользование
  // semantic.* — «успех» и «моё сообщение» не должны съезжать вместе.
  //
  // ДВА НАБОРА АКЦЕНТА, и это сознательно. Мессенджеры узнаваемо
  // синие, и синий пузырь читается как «переписка» без пояснений;
  // фирменный зелёный на сайте зарезервирован под ЕДИНСТВЕННЫЙ
  // акцент действия («Опубликовать», «Позвонить»), и заливать им
  // половину ленты значит обесценить его на остальных экранах.
  // Поэтому по умолчанию включён синий — он собран из брендовых
  // primary (#1565C0) и blue (#1E9AF0), то есть чужеродным не
  // выглядит. Зелёный вариант оставлен готовым: смена — одна строка
  // в bubbleMine ниже, разметку трогать не нужно.
  chat: {
    // Градиент своего пузыря: от тёмного края к светлому.
    // Направление (135deg) задаётся в Tailwind-утилите, здесь только
    // концы — чтобы значение цвета жило в одном месте.
    accentBlue: { from: '#2563EB', to: '#3B82F6' },
    // Запасной фирменный вариант: зелёный бренда и его светлый край.
    accentGreen: { from: '#16A34A', to: '#22C063' },

    // Пузырь собеседника. Сплошной светло-серый, а не прозрачный
    // чёрный из surface.*: лента стоит на белом, и полупрозрачность
    // здесь ничего не даёт, а точное значение #F1F3F5 держит пузырь
    // заметным, не превращая его в грязное пятно.
    bubblePeer: '#F1F3F5',
    // Фон ленты. Почти белый: воздух между пузырями важнее подложки,
    // но чистый #FFF сливал бы поле ввода с лентой.
    surface: '#FAFAFB',
    // Подложка поля ввода (pill) и капсулы даты.
    inputSurface: '#F1F3F5',
    // Тень своего пузыря — мягкая и цветная, под градиент: серая тень
    // на синем выглядит грязью.
    bubbleShadow:
      '0 1px 2px rgba(37,99,235,0.18), 0 4px 12px rgba(37,99,235,0.14)',
    // Радиус пузыря. 18px — не из шкалы radius: там ступени под
    // контролы (12) и контейнеры (16), а пузырь речи требует своей,
    // более круглой формы, иначе он читается как карточка.
    bubbleRadius: '18px',
    // Ширина ленты на десктопе. Строка длиннее ~720px читается
    // тяжело, а лента во всю ширину панели превращает короткие
    // реплики в редкие точки по краям.
    maxWidth: '720px',
  },

  // ------------------------------------------------------------
  // Тени (elevation).
  // ------------------------------------------------------------
  // Четыре ступени по назначению, а не по размеру: так в приложении
  // видно, какой Material elevation чему соответствует.
  shadow: {
    // Карточка объявления при наведении. Значение совпадает с
    // Tailwind shadow-md, который стоял в CarCard, — вид не меняется.
    card: '0 4px 6px -1px rgba(0,0,0,0.1), 0 2px 4px -2px rgba(0,0,0,0.1)',
    // Выпадающие списки: пикер марки, панель фильтров на десктопе.
    dropdown: '0 10px 15px -3px rgba(0,0,0,0.1), 0 4px 6px -4px rgba(0,0,0,0.1)',
    // Модальные окна и шторка фильтров на мобильном.
    modal: '0 20px 25px -5px rgba(0,0,0,0.1), 0 8px 10px -6px rgba(0,0,0,0.1)',
    // Залипающая шапка: тень появляется только при прокрутке.
    sticky: '0 1px 2px 0 rgba(0,0,0,0.05)',
  },

  // ------------------------------------------------------------
  // Слои по оси Z.
  // ------------------------------------------------------------
  // Единая лестница вместо разрозненных z-40 / z-50 в компонентах.
  // Порядок важен: шторка фильтров обязана перекрывать шапку, иначе
  // на мобильном шапка просвечивает поверх открытых фильтров.
  zIndex: {
    header: 40,
    filterSheet: 50,
    modal: 60,
    tooltip: 70,
  },

  // ------------------------------------------------------------
  // Анимации.
  // ------------------------------------------------------------
  // Две длительности: мгновенная реакция на действие (наведение,
  // нажатие) и переход слоя (открытие шторки). Кривая одна — ease-out:
  // элемент быстро стартует и мягко останавливается, это ощущается
  // отзывчивее симметричной ease-in-out.
  motion: {
    fast: '150ms',
    normal: '300ms',
    easing: 'cubic-bezier(0, 0, 0.2, 1)',
  },

  // Шрифт из приложения. Поддерживает кириллицу и латиницу — обязательное
  // требование для сербского рынка (sr latin + ru).
  font: {
    family: 'Montserrat',
    weight: {
      regular: 400,
      medium: 500,
      semibold: 600,
      bold: 700,
    },
  },
} as const;
