-- ============================================================
-- AUTO.RS — Миграция 0081: уточнение описания admin_list_cars.
-- ------------------------------------------------------------
-- Правка ОДНОГО comment on function. Ничего исполняемого не меняет.
--
-- ЗАЧЕМ ОТДЕЛЬНЫЙ ФАЙЛ РАДИ ОДНОЙ СТРОКИ. Миграция 0080 уже применена
-- к базе, и править её задним числом нельзя: файл в репозитории
-- разошёлся бы с тем, что реально выполнено. История миграций —
-- append-only ровно по той же причине, что и журнал администратора.
--
-- ЧТО ИСПРАВЛЯЕМ. В 0080 функция описана как «с двуалфавитным
-- поиском». Это неточно и вводит в заблуждение при чтении каталога
-- функций: f_normalize делает lower + unaccent, то есть снимает
-- регистр и диакритику («skoda», «Skoda», «Škoda» — одно и то же), но
-- НЕ транслитерирует кириллицу в латиницу. «Шкода» не найдёт «Škoda»,
-- «БМВ» не найдёт «BMW».
--
-- Это известное ограничение поиска всей площадки — оно одинаково у
-- каталога, приложения и админки, зафиксировано в RELEASE.md
-- («Отложено намеренно») и чинится разом для всех трёх, а не таблицей
-- соответствий внутри админской RPC.
--
-- Описание в pg_description читают те, кто разбирается в схеме без
-- доступа к репозиторию, — им нельзя обещать поведение, которого нет.
-- ============================================================

comment on function public.admin_list_cars(text, text, text, uuid, text, integer, integer)
  is 'Все объявления с фильтрами и поиском (нормализация как в каталоге: регистр и диакритика, без транслитерации между алфавитами); только для админа';
