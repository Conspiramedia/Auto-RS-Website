'use client';

// ============================================================
// RS AUTO — Форма объявления: подача и редактирование. Client Component.
// ============================================================
// ОДНА форма на два сценария (mode):
//   'create' — подача нового объявления с /sell;
//   'edit'   — правка своего объявления из кабинета
//              (/my/listing/[id]/edit).
//
// Почему один компонент, а не два. Поля, пикеры, маски, валидация и
// правила цен совпадают полностью: разошедшись на две формы, они
// разъехались бы при первой же правке — например, новое поле появилось
// бы только при подаче, и отредактировать его стало бы невозможно.
//
// Различия ровно три, и все они локальны:
//   1. в edit нет шага входа по SMS — сессия уже есть;
//   2. поля предзаполняются из get_car_details + get_car_images;
//   3. вызывается update_car_v3 вместо create_car_v3.
//
// Порядок шагов: Автомобиль → Детали → Фото → Контакты (вход по SMS).
// Вход намеренно последний: заставлять человека авторизоваться до того,
// как он что-то ввёл, — верный способ потерять продавца на первом экране.
// В режиме edit последнего шага нет: форма заканчивается фотографиями.
//
// Путь публикации (согласован с приложением, миграции 0035/0036/0040):
//   1. rpc_check_otp_quota(phone) — суточная квота SMS (5 на номер);
//   2. signInWithOtp / verifyOtp  — вход по телефону;
//   3. загрузка фото в бакет car-images, папка {uid}/… (требование RLS);
//   4. create_car_v2(...)          — создание объявления.
// Объявление создаётся со статусом 'moderation' (default в таблице cars) и
// появляется в каталоге только после approve_car администратором.
// ============================================================

import Link from 'next/link';
import { useRouter } from 'next/navigation';
import { useEffect, useMemo, useState } from 'react';

import { revalidateMyListings } from '@/app/my/actions';
import { getBrowserClient } from '@/lib/supabaseClient';
import { trackEvent } from '@/lib/analytics';
import {
  acceptPolicy,
  hasAcceptedPolicyHere,
  migrateGuestConsent,
} from '@/lib/consent';
import type { CarAvailability, CarCondition } from '@/lib/types';
import { SELECTABLE_CONDITIONS } from '@/lib/types';
import { ConditionIcon } from './ui/ConditionIcons';
import { segmentedClass } from './ui/segmented';
import type { DictKey, Locale } from '@/lib/i18n';
import { getT, localeHref } from '@/lib/i18n';
import { BRANDS, CITIES, YEAR_MIN, yearMax } from '@/lib/referenceData';
import {
  MAX_MILEAGE_DIGITS,
  MAX_PRICE_DIGITS,
  formatSerbianPhone,
  isValidSerbianContactPhone,
  parseThousands,
  SERBIAN_PHONE_PREFIX,
  serbianContactPhoneToE164,
  validateYear,
} from '@/lib/inputFormat';
import { usePhoneCaret } from '@/lib/usePhoneCaret';
import {
  DESCRIPTION_MAX,
  DESCRIPTION_MIN,
  descriptionLength,
  findContacts,
  splitByContacts,
  validateDescription,
} from '@/lib/contactGuard';
import {
  BODY_TYPES,
  ENGINE_VOLUME_VALUES,
  FUELS,
  TRANSMISSIONS,
} from '@/lib/types';
import type { SimilarListing } from '@/lib/types';
import ListPicker, { type PickerOption } from './ListPicker';
import PhotoPicker, { type PhotoItem } from './PhotoPicker';
import CloseButton from './ui/CloseButton';
import Alert from './ui/Alert';
import { fieldClass, fieldClassTextarea } from './ui/Field';
import NumberInput from './ui/NumberInput';
import {
  RESEND_DELAY_SEC,
  humanOtpError,
  supabaseErrorText,
} from '@/lib/otp';
import Button from './ui/Button';
import Card from './ui/Card';
import { SkeletonBox } from './ui/Skeleton';

type Props = {
  locale: Locale;
  // 'create' по умолчанию — страница подачи /sell не передаёт режим.
  mode?: 'create' | 'edit';
  // Идентификатор редактируемого объявления. Обязателен при mode='edit'
  // и не используется при подаче.
  carId?: string;
};

// Максимум фотографий объявления.
//
// Значение продублировано в базе — ограничение cars_photo_urls_limit
// (миграция 0105). Именно база здесь главная: до 0105 лимит держался
// только на клиентах, и прямой вызов RPC в обход формы записывал
// сколько угодно фотографий. Поднимая число, правьте ОБА места —
// иначе форма либо примет то, что база отклонит, либо перестанет
// пускать то, что база разрешает.
//
// С приложением значения намеренно РАЗОШЛИСЬ: его форма подачи
// принимает 10 (AppConstants.maxCarImages), сайт — 15. Backend
// пропускает и то и другое, просмотру объявления это не мешает.
// Синхронизация приложения — отдельная задача на его стороне.
const MAX_PHOTOS = 15;

// ГРАНИЦЫ ЧИСЛОВЫХ ПОЛЕЙ ШАГА «Детали».
//
// Задаются здесь, а не берутся из MAX_MILEAGE / MAX_PRICE
// (lib/inputFormat): те — защита от опечатки в десять знаков и потому
// намеренно щедрые (9 999 999). Это же — продуктовые границы, они уже.
//
// Пробег: миллион километров переживает разве что грузовик, и для
// легкового объявления такое число почти наверняка опечатка.
const MILEAGE_MAX = 1_000_000;
// Цена: ниже сотни евро машин не бывает даже на разбор. Верхняя
// граница — сто миллионов: коллекционные экземпляры и лоты аукционов
// столько стоят, и упираться в неё обычное объявление не должно.
// Цена НЕОБЯЗАТЕЛЬНА: пустое поле означает «Договорная», и границы
// проверяются только у заполненного.
const PRICE_MIN = 100;
const PRICE_MAX = 100_000_000;

// Девять цифр — ровно столько нужно для 100 000 000. Общий
// MAX_PRICE_DIGITS (7) здесь не подходит: поле просто не приняло бы
// восьмую цифру, и проверка границы никогда бы не сработала.
//
// Меняем ЛОКАЛЬНО, а не в lib/inputFormat: тот же MAX_PRICE_DIGITS
// стоит в фильтрах каталога (FilterPanel), где семи цифр достаточно —
// искать машину дороже десяти миллионов никто не станет, а лишние
// разряды только удлиняют поле.
//
// В базе ограничения нет: sale_price — numeric(12,2), то есть до
// 9 999 999 999.99, и сто миллионов проходят с запасом.
const PRICE_DIGITS = 9;

// Годы выпуска для пикера: свежие сверху, как в приложении.
// Считается один раз при загрузке модуля — список не меняется в течение
// сессии, и пересобирать его на каждый рендер незачем.
const YEARS: string[] = Array.from(
  { length: yearMax() - YEAR_MIN + 1 },
  (_, i) => String(yearMax() - i),
);

export default function SellForm({
  locale,
  mode = 'create',
  carId,
}: Props) {
  const t = getT(locale);

  // Каретка в поле телефона — только в конец номера, чтобы тап
  // в середину кода страны «+381 » не уводил туда цифры.
  const phoneCaret = usePhoneCaret();
  const router = useRouter();
  const supabase = getBrowserClient();

  const isEdit = mode === 'edit';

  const [step, setStep] = useState(1);
  // Загрузка исходных данных объявления в режиме правки. true на старте
  // только для edit: при подаче грузить нечего, и форма показывается
  // сразу.
  const [loading, setLoading] = useState(isEdit);
  // Ушло ли объявление на повторную модерацию после сохранения. Нужно
  // экрану успеха: решение принимает СЕРВЕР (сравнивает контент), и
  // угадывать его на клиенте нельзя.
  const [movedToModeration, setMovedToModeration] = useState(false);
  const [busy, setBusy] = useState(false);
  const [error, setError] = useState<string | null>(null);
  const [done, setDone] = useState(false);

  // Тип объявления. Определяет, какие поля цен показываются и что
  // уходит в create_car_v3. По умолчанию продажа — самый частый случай.
  // У объявления один тип сделки: продажа ИЛИ аренда. Вариант «и то и
  // другое» убран — одна машина, выставленная и на продажу, и в аренду,
  // подаётся двумя отдельными объявлениями. Защита от дублей это
  // разрешает: тип сделки входит в условие совпадения (миграция 0057).
  const [listingType, setListingType] = useState<'sale' | 'rent'>('sale');
  // ДОСТУПНОСТЬ АВТОМОБИЛЯ (миграция 0119). Салон торгует не только
  // тем, что стоит у него на площадке: часть машин он привозит под
  // клиента, часть уже куплена и едет к нему. Покупатель обязан
  // видеть это до звонка.
  //
  // ОДНО ПОЛЕ, А НЕ ДВА ЧЕКБОКСА: «под заказ» и «в пути»
  // взаимоисключающи, и выбор из трёх вариантов физически не даёт
  // отметить оба. Та же логика в базе — там это enum, а не пара
  // булевых флагов.
  //
  // Показывается ТОЛЬКО САЛОНУ (isDealer ниже). Частнику поля нет: у
  // него «привезу под заказ» означало бы объявление о машине, которой
  // у продавца нет. Проверка дублируется на стороне базы триггером
  // trg_cars_availability_dealer_only: клиент отвечает за удобство, а
  // не за правила.
  const [availability, setAvailability] =
    useState<CarAvailability>('in_stock');

  // СОСТОЯНИЕ АВТОМОБИЛЯ (0138) — ВТОРАЯ, НЕЗАВИСИМАЯ ОСЬ.
  // В отличие от доступности выше, поле показывается ВСЕМ продавцам:
  // битую машину или донора на разборку продаёт и частник, для него
  // это обычный сценарий, а не привилегия салона. Триггера,
  // ограничивающего значения, в базе нет намеренно.
  //
  // Оси не пересекаются и на уровне формы: у салона видны оба
  // селектора сразу, и «в пути» + «битый» — допустимое сочетание.
  const [condition, setCondition] = useState<CarCondition>('normal');
  // null — тип продавца ещё не прочитан. До этого момента поле не
  // рисуется вовсе: мелькнувший и исчезнувший переключатель хуже, чем
  // его появление на долю секунды позже.
  const [isDealer, setIsDealer] = useState<boolean | null>(null);

  // Шаг 1–2: данные автомобиля.
  const [brand, setBrand] = useState('');
  const [model, setModel] = useState('');
  // Модели выбранной марки. Грузятся из get_car_models — той же RPC,
  // что вызывает приложение, поэтому списки совпадают.
  const [modelList, setModelList] = useState<{ id: string; name: string }[]>([]);
  const [loadingModels, setLoadingModels] = useState(false);
  const [year, setYear] = useState('');
  const [price, setPrice] = useState('');
  const [rentPrice, setRentPrice] = useState('');
  const [deposit, setDeposit] = useState('');
  const [mileage, setMileage] = useState('');
  const [city, setCity] = useState('');
  const [bodyType, setBodyType] = useState('');
  const [transmission, setTransmission] = useState('');
  const [fuel, setFuel] = useState('');
  // Объём двигателя строкой («1.6»): в RPC уходит числом, но в
  // состоянии удобнее строка — пустая означает «не указан», а у
  // электромобиля так и должно быть.
  const [engineVolume, setEngineVolume] = useState('');
  const [description, setDescription] = useState('');

  // КОНТАКТЫ В ОПИСАНИИ (lib/contactGuard.ts, миграция 0135).
  //
  // Считаем на каждое изменение текста, но через useMemo: описание
  // длиной до 6000 символов прогоняется через дюжину регулярок, и
  // делать это на каждый ререндер формы (а их много — соседние поля,
  // прогресс загрузки фотографий) незачем.
  //
  // Это ВЕРХНИЙ слой, подсказка. Жёсткий барьер — в базе: клиент
  // обходится вызовом RPC напрямую, и последнее слово за сервером.
  // Здесь мы лишь показываем проблему сразу, а не после загрузки
  // фотографий на последнем шаге.
  const descriptionContacts = useMemo(
    () => findContacts(description),
    [description],
  );
  const hasDescriptionContacts = descriptionContacts.length > 0;

  // ОБЯЗАТЕЛЬНЫЕ ПОЛЯ ШАГА «Детали».
  //
  // Показываем ошибки не сразу, а после первой попытки уйти дальше:
  // краснеющая форма, которую человек ещё не начал заполнять, ругается
  // на него за то, чего он не делал. После первой попытки флаг остаётся
  // поднятым, и подсветка гаснет по мере заполнения полей.
  const [detailsTouched, setDetailsTouched] = useState(false);

  // Ключи словаря или null. Считаются всегда — флаг выше решает только
  // то, показывать ли их.
  const fieldErrors = useMemo(() => {
    const km = parseThousands(mileage);

    return {
      // Пробег обязателен. Ноль допустим (новая машина), поэтому
      // проверяем на null, а не на «ложность»: parseThousands('') даёт
      // null, а parseThousands('0') — ноль.
      mileage:
        km === null || km < 0 || km > MILEAGE_MAX
          ? ('sell_err_mileage' as const)
          : null,
      bodyType: bodyType ? null : ('sell_err_body' as const),
      transmission: transmission ? null : ('sell_err_transmission' as const),
      fuel: fuel ? null : ('sell_err_fuel' as const),
      // Объём УСЛОВНО обязателен: у электромобиля ДВС нет, и требовать
      // литры значило бы заставлять продавца выдумывать число. Пока
      // топливо не выбрано, объём не требуем — иначе форма ругалась бы
      // на поле, ответ по которому ещё не определён.
      engineVolume:
        fuel && fuel !== 'electric' && !engineVolume
          ? ('sell_err_engine' as const)
          : null,
    };
  }, [mileage, bodyType, transmission, fuel, engineVolume]);

  // Показывать ли конкретную ошибку: только после попытки перейти
  // дальше. Отдельная функция, чтобы условие не повторялось в разметке
  // пять раз.
  const errorFor = (key: keyof typeof fieldErrors) =>
    detailsTouched ? fieldErrors[key] : null;

  // Общая проверка описания: длина, ссылки, разметка, контакты.
  // Возвращает КЛЮЧ словаря или null — перевод подставляем здесь,
  // модуль про локали не знает.
  //
  // required: true — ОПИСАНИЕ ОБЯЗАТЕЛЬНО НА САЙТЕ. Оно часто
  // единственное место, где сказано про состояние и комплектацию:
  // характеристики есть у сорока одинаковых объявлений, а текст
  // продавца — нет. Объявление без него плохо и покупателю, и
  // модератору, которому нечего смотреть.
  //
  // Правило живёт ТОЛЬКО ЗДЕСЬ, не в базе. Барьер в триггере сработал
  // бы и для мобильного приложения, которое шлёт p_description
  // необязательным, — подача там начала бы падать. База остаётся с
  // прежним правилом «пустое допустимо, непустое от 30 символов»
  // (0136), и сайт лишь строже неё.
  const descriptionProblem = useMemo(
    () => validateDescription(description, { required: true }),
    [description],
  );

  // Длина без крайних пробелов — та же, что проверяют валидатор и
  // констрейнт базы. Счётчик обязан показывать именно её, иначе
  // «29 символов + пробел» выглядели бы как 30 допустимых.
  const descriptionLen = descriptionLength(description);

  // Шаг 3: фотографии. Набор смешанный: при подаче это только выбранные
  // файлы, при правке — ещё и уже загруженные снимки объявления
  // (см. PhotoItem в PhotoPicker).
  const [files, setFiles] = useState<PhotoItem[]>([]);

  // ПЛАШКА ГАСНЕТ, КАК ТОЛЬКО ЧЕЛОВЕК ВЗЯЛСЯ ЗА ИСПРАВЛЕНИЕ.
  //
  // Она отвечает на нажатие «Далее», и висеть после того, как продавец
  // начал исправлять, ей незачем: он уже понял, что от него хотят, а
  // красный текст под руками продолжает обвинять. Подсветка полей при
  // этом ОСТАЁТСЯ — она показывает, что ещё не заполнено, и гаснет
  // сама по мере заполнения.
  //
  // Работает на ДВУХ шагах с проверкой, и «взялся за исправление»
  // на каждом своё:
  //   шаг 2 «Детали»    — изменилось любое поле шага;
  //   шаг 3 «Фотографии» — изменился набор снимков (добавил или удалил).
  // Зависимости перечислены все разом: гасить плашку только на том
  // поле, к которому относится текст, значило бы объяснять
  // пользователю разницу, которой он не видит.
  //
  // ШАГИ 1 И 4 ИСКЛЮЧЕНЫ намеренно. Без этого эффект гасил бы чужие
  // сообщения: при правке объявления загрузка заполняет все поля
  // разом (и стёрла бы «Не удалось загрузить объявление»), а на шаге
  // контактов ошибка публикации пропала бы от касания полей,
  // оставшихся позади.
  //
  // На первом рендере эффект тоже срабатывает, но error там уже null,
  // и setError с тем же значением React изменением не считает.
  useEffect(() => {
    if (step !== 2 && step !== 3) return;
    setError(null);
  }, [
    step,
    mileage,
    bodyType,
    transmission,
    fuel,
    engineVolume,
    description,
    price,
    rentPrice,
    files,
  ]);
  // Прогресс отправки фотографий в хранилище, 0..100. Считается по
  // числу загруженных файлов, а не по байтам: Supabase Storage не
  // отдаёт события прогресса отдельного запроса, а по файлам
  // индикатор всё равно движется предсказуемо.
  const [uploadProgress, setUploadProgress] = useState(0);

  // Шаг 4: телефон и код.
  // Поле стартует с кодом страны: набирать «+381» руками незачем.
  const [phone, setPhone] = useState(SERBIAN_PHONE_PREFIX);
  // Адрес для входа. Гость подаёт объявление, ещё не имея аккаунта, —
  // он создаётся кодом на эту почту (см. sendCode). Телефон выше
  // остаётся контактом объявления и способом входа больше не является.
  const [email, setEmail] = useState('');
  // Номер, показанный продавцу на подтверждение. Пока не null — на
  // экране стоит вопрос «этот номер верный?», а публикация ждёт.
  //
  // ЗАЧЕМ ОТДЕЛЬНЫЙ ШАГ. Телефон больше не подтверждается кодом: SMS
  // с сайта не уходят, и номер попадает в объявление как есть. Опечатка
  // в нём означает, что покупатели просто не дозвонятся, а продавец
  // об этом никогда не узнает — объявление висит, звонков нет.
  // Поэтому номер показывается крупно и отдельно: единственная
  // оставшаяся проверка — глаза самого продавца.
  const [phoneToConfirm, setPhoneToConfirm] = useState<string | null>(null);
  const [code, setCode] = useState('');
  const [codeSent, setCodeSent] = useState(false);
  // Номер, на который реально ушёл код, в формате E.164. Держим отдельно
  // от поля ввода: verifyOtp обязан получить ТОТ ЖЕ номер, что и
  // signInWithOtp, иначе Supabase не найдёт код.
  const [sentTo, setSentTo] = useState('');
  // Успешное подтверждение номера. Между verifyOtp и публикацией есть
  // загрузка фотографий — на это время пользователю нужен признак, что
  // код принят, иначе долгая загрузка выглядит как зависшая проверка.
  const [phoneVerified, setPhoneVerified] = useState(false);

  // Согласие с условиями и политикой. Без него код не отправляется —
  // тот же порядок, что в приложении (login_screen.dart: политика
  // принимается ДО sendOtp, а не после).
  //
  // Начальное значение подставляется из localStorage в useEffect ниже:
  // при первом рендере обращаться к нему нельзя (форма рендерится и на
  // сервере), а расхождение разметки сервера и клиента ломает гидрацию.
  const [agreed, setAgreed] = useState(false);

  // Уже вошедший продавец. Сессия Supabase живёт между визитами
  // (persistSession в lib/supabaseClient), и человеку, подающему второе
  // объявление, незачем снова получать SMS: код нужен для СОЗДАНИЯ
  // сессии, а она уже есть.
  //
  // null — проверка ещё идёт: до её конца шаг 4 не показывает ни блок
  // входа, ни кнопку публикации, иначе на долю секунды мелькнёт
  // «Отправить код» у того, кто давно вошёл.
  const [signedIn, setSignedIn] = useState<boolean | null>(null);

  // Есть ли телефон у ВОШЕДШЕГО аккаунта. Раньше это подразумевалось:
  // единственным способом входа был SMS, значит номер есть у каждого.
  // С появлением входа по почте (миграция 0082) это перестало быть
  // правдой — администратор площадки зарегистрирован без телефона.
  //
  // Телефон при этом обязателен для объявления: покупателю нужно, куда
  // звонить. Поэтому у вошедшего без номера поле показывается, как у
  // гостя, но SMS-подтверждение не запрашивается — сессия уже есть, и
  // номер идёт прямо в объявление.
  const [accountPhone, setAccountPhone] = useState<string | null>(null);

  // Обратный отсчёт до повторной отправки. Задержка общая с входом в
  // кабинет (lib/otp.ts): расхождение здесь означало бы, что одна и та
  // же квота SMS расходуется по разным правилам на разных экранах.
  // Хранится момент, когда отправка снова разрешена: при возврате на
  // вкладку из фона таймер по «оставшимся секундам» отстал бы, а по
  // метке времени пересчёт всегда верный.
  const [resendAt, setResendAt] = useState(0);
  const [resendIn, setResendIn] = useState(0);
  // Сообщение об успешной повторной отправке (в приложении — снек).
  const [notice, setNotice] = useState<string | null>(null);

  // Открытие формы подачи — вершина воронки продавца. Считается один
  // раз за монтирование: пустой список зависимостей, а не при каждом
  // шаге, иначе одна подача давала бы четыре события.
  useEffect(() => {
    trackEvent('sell_start');
  }, []);

  // Определение уже вошедшего продавца и ранее принятой политики.
  // Оба факта живут на устройстве и читаются ТОЛЬКО на клиенте.
  useEffect(() => {
    let cancelled = false;

    (async () => {
      const { data } = await supabase.auth.getSession();
      if (cancelled) return;

      const uid = data.session?.user.id ?? null;
      setSignedIn(uid !== null);

      // ТЕЛЕФОН БЕРЁТСЯ ИЗ ПРОФИЛЯ, А НЕ ИЗ СЕССИИ.
      //
      // Раньше здесь стоял session.user.phone — поле auth-аккаунта,
      // которое заполняется ТОЛЬКО при входе по SMS. После перехода
      // на почтовый вход (0106) оно пустое у всех, и форма спрашивала
      // бы номер при каждой подаче.
      //
      // get_profile_phone отдаёт контакт из profiles — тот, что
      // продавец ввёл при первой подаче. Ошибку глушим: недоступная
      // RPC не должна мешать подать объявление, поле просто покажется
      // пустым и номер спросят заново.
      if (uid) {
        // Тип продавца — прямым select под политикой profiles_select_own:
        // это своя строка, и заводить ради одного поля RPC незачем.
        // Ошибку глушим: недоступный профиль не должен мешать подаче,
        // поле «на заказ» просто не покажется.
        const { data: profileRow } = await supabase
          .from('profiles')
          .select('seller_kind')
          .eq('id', uid)
          .maybeSingle();

        if (!cancelled) {
          setIsDealer(profileRow?.seller_kind === 'dealer');
        }

        const { data: savedPhone } = await supabase.rpc('get_profile_phone');
        if (!cancelled && typeof savedPhone === 'string' && savedPhone) {
          setAccountPhone(savedPhone);
          // Подставляем в поле: продавец видит свой номер и может его
          // поправить, а не вводить заново.
          setPhone(formatSerbianPhone(savedPhone));
        }
      }

      // Согласие могло быть дано ещё гостем — переносим на аккаунт,
      // иначе тот же человек увидит непринятый чекбокс.
      if (uid) migrateGuestConsent(uid);
      // Проверяем и аккаунт, и гостя: документы принимаются один раз
      // на устройстве, а не при каждом входе. Без гостевой ветки
      // продавец без активной сессии снова видел пустой чекбокс.
      setAgreed(hasAcceptedPolicyHere(uid));
    })();

    return () => {
      cancelled = true;
    };
    // supabase — синглтон, ссылка стабильна: эффект выполняется однажды.
  }, [supabase]);

  // ------------------------------------------------------------
  // Режим правки: предзаполнение формы данными объявления.
  // ------------------------------------------------------------
  // Два запроса идут ПАРАЛЛЕЛЬНО (Promise.all): карточка и её фотографии
  // независимы, и последовательное ожидание удвоило бы время до
  // появления формы.
  //
  // Права проверяет сервер: get_car_details отдаёт непубличные статусы
  // только владельцу и админу (миграция 0048). Страница дополнительно
  // сверяет user_id и отдаёт 404 на чужое объявление — здесь это уже
  // гарантировано, поэтому форма просто показывает то, что пришло.
  useEffect(() => {
    if (!isEdit || !carId) return;

    let cancelled = false;

    (async () => {
      const [detailsResult, imagesResult] = await Promise.all([
        supabase.rpc('get_car_details', { p_car_id: carId }),
        supabase.rpc('get_car_images', { p_car_id: carId }),
      ]);

      if (cancelled) return;

      const car = (detailsResult.data ?? [])[0] as
        | Record<string, unknown>
        | undefined;

      if (detailsResult.error || !car) {
        setError(t('edit_err_load'));
        setLoading(false);
        return;
      }

      // Тип сделки. Объявление «и продажа, и аренда» (такие создавало
      // приложение через v2) приводим к продаже: форма сайта предлагает
      // только один тип, а продажа — основная сделка такой карточки.
      setListingType(car.is_for_rent && !car.is_for_sale ? 'rent' : 'sale');
      setAvailability(
        (car.availability as CarAvailability | undefined) ?? 'in_stock',
      );
      // Состояние (0138). Отсутствующее значение — 'normal': так же
      // ведёт себя база, и объявление, поданное до миграции, не
      // получит в форме правки чужую пометку.
      setCondition((car.condition as CarCondition | undefined) ?? 'normal');

      setBrand((car.brand as string) ?? '');
      setModel((car.model as string) ?? '');
      setYear(car.year != null ? String(car.year) : '');
      setCity((car.city as string) ?? '');
      setBodyType((car.body_type as string) ?? '');
      setTransmission((car.transmission as string) ?? '');
      setFuel((car.fuel as string) ?? '');
      // toFixed(1) обязателен: БД отдаёт numeric как 1.6 или как 2,
      // а в списке значения записаны с одним знаком — без приведения
      // «2» не совпало бы с пунктом «2.0» и поле выглядело бы пустым.
      setEngineVolume(
        car.engine_volume != null
          ? Number(car.engine_volume).toFixed(1)
          : '',
      );
      setDescription((car.description as string) ?? '');

      // Числовые поля хранят ЧИСТЫЕ ЦИФРЫ («125000»): разделители
      // рисует NumberInput при показе, наружу они не выходят. Поэтому
      // предзаполняем просто строкой числа — форматировать нечего.
      // ТЕЛЕФОН ОБЪЯВЛЕНИЯ. Поле не показывается тому, у кого номер
      // есть в аккаунте, но предзаполнять его обязательно: при
      // сохранении форма берёт номер из сессии, а у вошедшего ПО
      // ПОЧТЕ (миграция 0082) в сессии телефона нет вовсе. Без этой
      // строки на его месте оказывался пустой префикс «+381», не
      // проходил нормализацию, и правка падала на шаге verify
      // ошибкой «Не удалось подтвердить код» — при том, что никакого
      // кода в режиме правки не запрашивают.
      //
      // Источник — сам объявление: этот номер уже прошёл проверку при
      // подаче, и именно он должен остаться в карточке, если продавец
      // его не менял.
      setPhone(
        car.contact_phone
          ? formatSerbianPhone(String(car.contact_phone))
          : SERBIAN_PHONE_PREFIX,
      );

      setMileage(car.mileage != null ? String(car.mileage) : '');
      setPrice(
        car.sale_price != null
          ? String(Math.round(Number(car.sale_price)))
          : '',
      );
      setRentPrice(
        car.rent_price_daily != null
          ? String(Math.round(Number(car.rent_price_daily)))
          : '',
      );
      setDeposit(
        car.deposit_amount != null && Number(car.deposit_amount) > 0
          ? String(Math.round(Number(car.deposit_amount)))
          : '',
      );

      // Существующие фотографии в порядке order_index — его задаёт RPC.
      //
      // ОШИБКА ВЫБОРКИ ФОТО — ФАТАЛЬНА, и это изменение прежнего
      // решения. Раньше она молча игнорировалась ради того, чтобы
      // правка описания не срывалась из-за картинок. Замысел разумный,
      // следствие — потеря данных: пикер оставался пустым, продавец
      // видел объявление без единого снимка и, нажав «Сохранить»,
      // отправлял p_photo_urls пустым массивом. update_car_v3 делает
      // ПОЛНУЮ ЗАМЕНУ набора, то есть все фотографии удалялись —
      // из-за оборвавшегося запроса, о котором никто не сообщил.
      //
      // Оборвать запрос при этом легко: достаточно свернуть вкладку на
      // время загрузки (браузер отдаёт ERR_NETWORK_IO_SUSPENDED).
      if (imagesResult.error) {
        setError(t('edit_err_load'));
        setLoading(false);
        return;
      }

      const urls = (imagesResult.data ?? []) as { image_url: string }[];
      setFiles(
        urls.map((row): PhotoItem => ({ kind: 'url', url: row.image_url })),
      );

      // Список моделей выбранной марки: без него пикер модели окажется
      // пустым, и продавец не сможет сменить модель, не выбрав марку
      // заново.
      if (car.brand) {
        const { data: models } = await supabase.rpc('get_car_models', {
          p_brand_name: car.brand as string,
        });
        if (!cancelled) {
          setModelList((models ?? []) as { id: string; name: string }[]);
        }
      }

      setLoading(false);
    })();

    return () => {
      cancelled = true;
    };
    // ЗАВИСИМОСТИ: только то, что действительно определяет, ЧТО
    // грузить. supabase — синглтон, ссылка стабильна.
    //
    // t В СПИСКЕ БЫТЬ НЕ ДОЛЖНА, хотя линтер её и требует. getT
    // возвращает НОВУЮ стрелочную функцию при каждом вызове, а вызов
    // стоит в теле компонента (const t = getT(locale)) — значит на
    // каждом рендере ссылка новая. С t в зависимостях эффект
    // перезапускался ПОСЛЕ КАЖДОГО РЕНДЕРА: продавец печатал символ в
    // поле города, происходил рендер, эффект снова читал объявление и
    // вызывал setCity/setMileage/setPrice, затирая только что
    // введённое исходными значениями. Снаружи это выглядело так, будто
    // форма не даёт изменить ни одного поля, а сохранение отвечало
    // «содержимое не изменилось» — потому что к моменту отправки в
    // состоянии действительно лежали прежние значения.
    //
    // Побочный эффект того же дефекта — шквал повторных запросов
    // get_car_details/get_car_images на каждое нажатие клавиши,
    // вплоть до ERR_NETWORK_IO_SUSPENDED от браузера.
    //
    // t используется внутри только для одного текста ошибки и от
    // локали в пределах смонтированной формы не меняется: локаль
    // задаётся маршрутом, а смена маршрута размонтирует компонент.
    //
    // Директива ниже сейчас ни на что не влияет — ESLint в проекте не
    // установлен (см. package.json). Оставлена на случай, когда его
    // заведут: без неё правило exhaustive-deps потребует вернуть t в
    // список и воспроизведёт этот же баг.
    // eslint-disable-next-line react-hooks/exhaustive-deps
  }, [isEdit, carId, supabase]);

  useEffect(() => {
    if (resendAt === 0) return;

    // Пересчёт остатка от метки времени. Один интервал на весь отсчёт.
    const tick = () => {
      const left = Math.ceil((resendAt - Date.now()) / 1000);
      setResendIn(left > 0 ? left : 0);
    };

    tick();
    const id = setInterval(tick, 1000);
    return () => clearInterval(id);
  }, [resendAt]);

  // ---------- Своё похожее объявление (уровень 1 защиты от дублей) ----------
  // Найденное объявление того же продавца с тем же brand+model+year.
  // null — либо ещё не искали, либо не нашли.
  //
  // ЭТО ПРЕДУПРЕЖДЕНИЕ, А НЕ БЛОКИРОВКА. Кнопка «Далее» остаётся
  // рабочей: у продавца может быть законная причина подать вторую
  // такую же машину, и решать это должен он, а не форма. Жёсткий
  // отказ живёт на бэкенде (trg_cars_prevent_duplicate, 0093) и
  // сработает при публикации.
  //
  // Смысл плашки — показать своё же объявление НА ПЕРВОМ ШАГЕ, до
  // фотографий и SMS. Раньше продавец узнавал о дубле только отказом
  // на последнем шаге, потратив на форму несколько минут.
  const [duplicate, setDuplicate] = useState<SimilarListing | null>(null);
  // Закрыл ли продавец плашку кнопкой «Всё равно подать новое».
  // Держим отдельно от duplicate: при смене марки/модели/года поиск
  // идёт заново, и скрытие не должно переезжать на другую машину.
  const [dupDismissed, setDupDismissed] = useState(false);

  // ---------- Поиск своего похожего объявления ----------
  // Идёт, когда заполнена вся тройка марка + модель + год: по неполному
  // ключу искать нечего, а дёргать сервер на каждую букву в поле марки
  // незачем.
  //
  // ТОЛЬКО ПРИ ПОДАЧЕ. В режиме правки плашка вредна: продавец
  // редактирует своё же объявление, и оно само окажется «похожим» —
  // предупреждение указывало бы на ту карточку, которая открыта.
  //
  // ТОЛЬКО ВОШЕДШЕМУ. У гостя объявлений нет по определению, а RPC
  // читает по auth.uid() и без сессии вернула бы пустой список.
  useEffect(() => {
    if (isEdit || !signedIn) return;

    const y = Number(year);
    if (!brand.trim() || !model.trim() || !y) {
      setDuplicate(null);
      return;
    }

    // Ключ изменился — прежнее скрытие плашки больше не действует:
    // продавец закрывал предупреждение про другую машину.
    setDupDismissed(false);

    let cancelled = false;

    (async () => {
      const { data, error: rpcError } = await supabase.rpc(
        'get_my_similar_listings',
        { p_brand: brand.trim(), p_model: model.trim(), p_year: y },
      );

      if (cancelled) return;

      // Ошибку не показываем и в консоль не пишем: плашка —
      // необязательная подсказка, и сбой её загрузки не должен ни
      // мешать подаче, ни выглядеть как проблема с формой. Жёсткая
      // проверка всё равно ждёт на сервере.
      if (rpcError) {
        setDuplicate(null);
        return;
      }

      const found = (data ?? []) as SimilarListing[];
      setDuplicate(found[0] ?? null);
    })();

    return () => {
      cancelled = true;
    };
  }, [brand, model, year, isEdit, signedIn, supabase]);

  // Honeypot: поле скрыто от человека и пустое у него всегда. Заполненное
  // значение — признак бота, и такую отправку мы молча не публикуем.
  const [website, setWebsite] = useState('');

  // Классы поля ввода — из общего паттерна (components/ui/Field).
  // Раньше эта строка была скопирована в четырёх формах.
  const field = fieldClass;
  const fieldTextarea = fieldClassTextarea;

  // Выбор марки: сбрасывает модель и подтягивает её список — тот же
  // порядок действий, что в create_car_screen.dart приложения.
  async function handleBrandChange(next: string) {
    setBrand(next);
    setModel('');
    setModelList([]);

    if (!next) return;

    setLoadingModels(true);
    const { data, error: rpcError } = await supabase.rpc('get_car_models', {
      p_brand_name: next,
    });
    // Ошибку не показываем: модель — необязательное уточнение, и без
    // списка продавец сможет ввести своё значение через «Указать».
    setModelList(rpcError ? [] : ((data ?? []) as { id: string; name: string }[]));
    setLoadingModels(false);
  }

  // Нормализация номера в E.164 живёт в lib/inputFormat. Своя
  // реализация здесь была слабее: она принимала любой набор из 9–15
  // цифр, поэтому номер чужой страны проходил проверку.
  //
  // ГРАНИЦА — КОНТАКТНАЯ, А НЕ ВХОДНАЯ. Раньше здесь стояла
  // serbianPhoneToE164, пропускающая только мобильные: номер был
  // способом входа, и SMS-код на городской не пришёл бы. После
  // перехода на почтовый вход (0106) телефон в форме — контакт для
  // покупателя, а у автосалона это чаще 011 в Белграде или 021 в
  // Нови-Саде. serbianContactPhoneToE164 повторяет ограничение базы
  // cars_contact_phone_serbian, которое и решает судьбу номера, —
  // форма больше не отказывает в том, что база принимает.
  function normalizePhone(raw: string): string {
    return serbianContactPhoneToE164(raw) ?? '';
  }

  // ---------- Отправка кода на почту ----------
  // resend = true — повторная отправка на тот же адрес: квоту
  // проверяем так же.
  // Гость подаёт объявление, ещё не имея аккаунта: он создаётся самим
  // входом. Раньше входом был SMS-код на номер продавца, теперь — код
  // на почту (миграции 0082 и 0106), потому что SMS с сайта не уходят:
  // Twilio требует одобренного Compliance Profile для сербских номеров.
  //
  // ТЕЛЕФОН ПРИ ЭТОМ ВСЁ РАВНО СПРАШИВАЕТСЯ, но выше по форме и как
  // контакт для покупателя, а не как способ входа: подтверждать его
  // кодом больше не нужно, продавец проверяет номер глазами.
  async function sendCode(resend = false) {
    setError(null);
    setNotice(null);

    // Согласие — обязательное условие ДО создания аккаунта: он
    // заводится самим входом, поэтому политика принимается здесь.
    if (!agreed) {
      setError(t('legal_consent_required'));
      return;
    }

    // Фиксируем принятие текущей редакции. Пока пользователь — гость;
    // на его uid согласие переедет после успешного входа.
    acceptPolicy(null);

    const clean = (resend && sentTo ? sentTo : email).trim().toLowerCase();

    // Грубая проверка формы адреса — до запроса: незачем тратить
    // квоту и тревожить GoTrue заведомой опечаткой.
    if (!/^[^@\s]+@[^@\s]+\.[a-z]{2,}$/i.test(clean)) {
      setError(t('auth_email_invalid'));
      return;
    }

    setBusy(true);
    try {
      // Квота проверяется ДО отправки: RPC сама пишет журнал.
      // В отличие от входа в кабинет здесь НЕ используется
      // rpc_check_email_login: тот пускает только существующие
      // профили, а подача объявления гостем — это регистрация.
      const { error: otpError } = await supabase.auth.signInWithOtp({
        email: clean,
        options: {
          // Аккаунт создаётся: это первый вход продавца на площадку.
          // Отличие от AuthGate, где shouldCreateUser: false —
          // там вход в УЖЕ существующий кабинет.
          shouldCreateUser: true,
        },
      });
      if (otpError) throw new Error(supabaseErrorText(otpError));

      setSentTo(clean);
      setCodeSent(true);

      // Продавец дошёл до подтверждения адреса — ключевая точка
      // воронки: разрыв между sell_start и otp_sent показывает, где
      // теряются люди. Повторную отправку не считаем: то же событие.
      // Адрес в аналитику НЕ передаётся.
      if (!resend) trackEvent('otp_sent', { listing_type: listingType });

      // Запускаем отсчёт до следующей отправки.
      setResendAt(Date.now() + RESEND_DELAY_SEC * 1000);
      if (resend) setNotice(t('otp_resent'));
    } catch (e) {
      setError(humanOtpError(e, t));
    } finally {
      setBusy(false);
    }
  }

  // Возврат к вводу номера — как «Изменить номер» в приложении.
  function changeNumber() {
    setCodeSent(false);
    setCode('');
    setSentTo('');
    setResendAt(0);
    setResendIn(0);
    setError(null);
    setNotice(null);
  }

  // ---------- Сверка номера перед публикацией ----------
  // Промежуточный шаг между кнопкой «Опубликовать» и самой публикацией.
  // Показывает номер, который уйдёт в объявление, и ждёт подтверждения.
  //
  // ПОЧЕМУ НЕ ПРИ ПЕРЕХОДЕ МЕЖДУ ШАГАМИ. Телефон вводится на том же
  // четвёртом шаге, где стоит кнопка публикации, — «перехода» после
  // его заполнения просто нет. Сверка привязана к последнему действию,
  // после которого номер уже не изменить.
  //
  // В режиме правки шаг пропускается: продавец правит существующее
  // объявление, номер в нём уже проверен при первой подаче, и
  // спрашивать о нём при каждой смене цены — навязчиво.
  function requestSubmit() {
    if (isEdit) {
      void submit();
      return;
    }

    const e164 = normalizePhone(phone);
    if (e164 === '') {
      setError(t('otp_err_phone'));
      return;
    }

    setError(null);
    setPhoneToConfirm(e164);
  }

  // ---------- Подтверждение кода и публикация ----------
  async function submit() {
    setError(null);

    // Бот заполнил скрытое поле — показываем «успех», но ничего не создаём.
    // Молчаливый отказ не даёт скрипту понять, что его отсекли.
    if (website.trim() !== '') {
      setDone(true);
      return;
    }

    setBusy(true);

    // Шаг, на котором находится публикация. Нужен единственному месту —
    // отчёту об ошибке в catch: по сообщению Supabase не всегда видно,
    // отклонён код, не прошла загрузка фото или упала сама RPC.
    // Персональные данные (uid, телефон) в лог не попадают.
    let stage: 'verify' | 'upload' | 'create' = 'verify';

    try {
      // ТЕЛЕФОН СЧИТАЕТСЯ ИЗ ПОЛЯ ФОРМЫ, А НЕ ИЗ sentTo.
      //
      // Здесь стояло `sentTo || normalizePhone(phone)` — наследие
      // SMS-входа, когда sentTo хранил НОМЕР, на который ушёл код, и
      // брать его оттуда было правильно. После перехода на почтовый
      // вход (0106) sentTo хранит АДРЕС ПОЧТЫ, и у гостя, подающего
      // первое объявление, это выражение клало почту в contactPhone:
      // адрес уходил и в create_car_v3 контактом объявления, и в
      // set_profile_phone телефоном профиля. Ограничение
      // cars_contact_phone_serbian такую строку отклоняло, и подача
      // падала невнятной ошибкой из PostgREST.
      //
      // Поле ввода — единственный источник номера в обоих сценариях:
      // у гостя он введён вручную, у вошедшего подставлен из профиля
      // (get_profile_phone при загрузке).
      //
      // Проверка стоит ДО ветвления: create_car_v3 требует непустой
      // номер (ограничение cars_contact_phone_serbian), и отсечь его
      // здесь честнее, чем получить невнятную ошибку из PostgREST
      // после загрузки всех фотографий. Раньше она стояла только в
      // ветке вошедшего, а гость доходил до базы с чем угодно.
      //
      // Текст именно про НОМЕР, а не про код подтверждения: в режиме
      // правки никакого кода форма не запрашивает, и сообщение о нём
      // сбивало с толку полностью.
      const contactPhone = normalizePhone(phone);
      if (!contactPhone) throw new Error(t('otp_err_phone'));

      // 1) Вход. У продавца, подающего не первое объявление, сессия уже
      // есть — код не нужен вовсе: он служит для СОЗДАНИЯ сессии, а не
      // для подтверждения каждой публикации. Лишний verifyOtp здесь
      // отклонялся бы (кода нет) и жёг суточную квоту.
      let uid: string | undefined;

      // В режиме правки шага входа нет вовсе: в кабинет пускает только
      // серверная проверка сессии (app/my/layout), поэтому здесь она
      // заведомо есть. Ветка signedIn ниже обрабатывает и этот случай —
      // берёт uid и телефон из текущей сессии.
      if (isEdit || signedIn) {
        const { data: current } = await supabase.auth.getSession();
        uid = current.session?.user.id;
      } else {
        const { data: auth, error: verifyError } =
          await supabase.auth.verifyOtp({
            email: sentTo,
            token: code.trim(),
            type: 'email',
          });
        if (verifyError) throw new Error(supabaseErrorText(verifyError));
        if (!auth.session) throw new Error(t('otp_err_failed'));
        uid = auth.user?.id;

        // Согласие давалось гостем — переносим на созданный аккаунт,
        // чтобы при следующей подаче политику не спрашивали снова.
        if (uid) migrateGuestConsent(uid);
      }

      // Сессия обязана быть: без неё RLS отклонит и загрузку фото,
      // и create_car_v3 — объявление осталось бы без владельца.
      //
      // Тексты разные по режимам. При подаче сюда приводит неудачное
      // подтверждение кода, при правке — истёкшая сессия: кода там не
      // запрашивают вовсе, и сообщение про него отправляло бы искать
      // причину не там.
      if (!uid) {
        throw new Error(t(isEdit ? 'edit_err_session' : 'otp_err_failed'));
      }

      setPhoneVerified(true);
      // Дальше начинается загрузка фотографий: если что-то упадёт,
      // отчёт об ошибке должен назвать именно этот шаг.
      stage = 'upload';

      // 2) Фотографии. Массив photoUrls собирается В ПОРЯДКЕ, который
      // продавец задал в PhotoPicker: первая ссылка становится обложкой
      // объявления в каталоге.
      //
      // При правке набор смешанный. Уже загруженные снимки берём по
      // готовому адресу — заново отправлять их в хранилище незачем:
      // это лишний трафик и дубли одного файла в бакете. Грузятся
      // только те, что продавец добавил сейчас.
      //
      // ЗАГРУЗКА ИДЁТ ПО ТРИ ФАЙЛА ПАРАЛЛЕЛЬНО. Раньше файлы уходили
      // строго по одному, и на мобильной сети это была основная часть
      // ожидания: каждый запрос почти всё время ждёт сеть, а не процессор.
      // Три потока дают выигрыш в 2–3 раза и при этом не забивают канал
      // так, чтобы страница перестала отвечать.
      //
      // ПОРЯДОК РЕЗУЛЬТАТА ОБЯЗАН СОВПАДАТЬ С ПОРЯДКОМ В ФОРМЕ: индекс
      // в photoUrls становится order_index в car_images, а нулевой
      // элемент — обложкой объявления в каталоге. Поэтому результат
      // кладётся В ЯЧЕЙКУ ПО ИНДЕКСУ, а не push-ем: при параллельной
      // загрузке файлы финишируют в произвольной очерёдности, и push
      // перемешал бы фотографии — обложкой стал бы случайный снимок.
      const photoUrls: string[] = new Array<string>(files.length);
      setUploadProgress(0);

      // Прогресс считаем по НОВЫМ файлам: существующие не грузятся, и
      // включать их в знаменатель значило бы показать «50%» там, где
      // работы нет вовсе.
      const pending = files
        .map((item, index) => ({ item, index }))
        .filter((entry) => entry.item.kind === 'file');
      const toUpload = pending.length;
      let uploaded = 0;

      // Уже загруженные (режим правки) — сразу на свои места.
      files.forEach((item, index) => {
        if (item.kind === 'url') photoUrls[index] = item.url;
      });

      // Общий курсор по очереди: три воркера разбирают из него задачи,
      // пока они не кончатся. Это ровно «три одновременно», тогда как
      // разбиение на куски по три ждало бы самый медленный файл в куске.
      let cursor = 0;
      const UPLOAD_CONCURRENCY = 3;

      async function uploadWorker() {
        for (;;) {
          const next = cursor++;
          if (next >= pending.length) return;

          const { item, index } = pending[next];
          if (item.kind !== 'file') continue;

          const file = item.file;
          // Расширение всегда .jpg: preparePhoto перекодирует любой
          // вход в JPEG и переименовывает файл. Запасной вариант
          // оставлен на случай, если сюда попадёт файл мимо пикера.
          const ext = file.name.split('.').pop()?.toLowerCase() || 'jpg';
          // Путь ОБЯЗАН начинаться с uid: политика car_images_insert_own
          // разрешает запись только в свою папку. Индекс в имени — из
          // позиции в форме, иначе два воркера могли бы взять одну
          // метку времени и перетереть файл друг друга.
          const path = `${uid}/${Date.now()}_${index}.${ext}`;

          const { error: uploadError } = await supabase.storage
            .from('car-images')
            .upload(path, file, { upsert: false });
          if (uploadError) throw new Error(supabaseErrorText(uploadError));

          const { data: pub } = supabase.storage
            .from('car-images')
            .getPublicUrl(path);
          photoUrls[index] = pub.publicUrl;

          // Прогресс после КАЖДОГО файла: при десяти снимках это
          // единственная обратная связь на протяжении десятков секунд.
          uploaded += 1;
          setUploadProgress(Math.round((uploaded / toUpload) * 100));
        }
      }

      // Promise.all, а не allSettled: отказ хотя бы одного файла обязан
      // прервать публикацию — объявление с молча пропавшей фотографией
      // хуже, чем понятная ошибка. Остальные воркеры при этом
      // дорабатывают свои текущие запросы, лишние файлы подчистит
      // фоновая уборка временной папки.
      await Promise.all(
        Array.from(
          { length: Math.min(UPLOAD_CONCURRENCY, toUpload) },
          uploadWorker,
        ),
      );

      stage = 'create';

      // 3) Запись. Обе функции — create_car_v3 (0055) и update_car_v3
      // (0067) — принимают ОДИНАКОВЫЙ набор полей с раздельными ценами
      // продажи и аренды. Поэтому параметры собираются один раз, а
      // отличается только имя RPC и добавленный при правке p_car_id.
      //
      // Прежняя update_car_v2 принимала одну цену и при 'both'
      // копировала её в обе колонки — ровно та ошибка, ради которой
      // появилась v3.
      const payload = {
        p_listing_type: listingType,
        // У аренды доступность не имеет смысла: сдают ту машину,
        // которая есть, — поэтому туда всегда уходит 'in_stock'.
        p_availability: listingType === 'sale' ? availability : 'in_stock',
        // Состояние уходит ВСЕГДА и для любой витрины: сдают в аренду
        // тоже не всякую машину, а условие «без документов» для
        // арендатора значит ровно то же, что для покупателя.
        p_condition: condition,
        p_brand: brand.trim(),
        p_model: model.trim(),
        p_year: Number(year),
        // Поля цен и пробега хранят чистые цифры, но через
        // parseThousands они идут всё равно: он переводит пустую строку
        // в null, а не в 0. Ноль пробега БД поймёт как «новая машина»,
        // ноль цены — как «отдаю бесплатно».
        p_mileage: parseThousands(mileage),
        // Цена продажи нужна только продающим объявлениям, суточная
        // ставка — только сдающимся. Лишние значения не отправляем,
        // чтобы в базе не осталось цены от неактуального типа.
        p_sale_price:
          listingType === 'sale' ? parseThousands(price) : null,
        p_rent_price_daily:
          listingType === 'rent' ? parseThousands(rentPrice) : null,
        p_deposit_amount:
          listingType === 'rent' ? (parseThousands(deposit) ?? 0) : 0,
        p_currency: 'EUR',
        p_city: city.trim(),
        p_lat: null,
        p_lng: null,
        p_photo_urls: photoUrls,
        p_body_type: bodyType || null,
        p_transmission: transmission || null,
        p_fuel: fuel || null,
        // Пустая строка — это «не указан» (электромобиль), а не ноль.
        p_engine_volume: engineVolume ? Number(engineVolume) : null,
        p_description: description.trim() || null,
        p_phone: contactPhone,
      };

      if (isEdit) {
        const { data, error: updateError } = await supabase.rpc(
          'update_car_v3',
          { p_car_id: carId, ...payload },
        );
        if (updateError) throw new Error(supabaseErrorText(updateError));

        // Ушло ли объявление на повторную проверку, решает СЕРВЕР:
        // он сравнивает новый контент со старым (миграция 0067).
        // Клиент только читает результат — повторять это сравнение
        // здесь значило бы завести второй источник истины.
        const updated = (data ?? [])[0] as { status?: string } | undefined;
        setMovedToModeration(updated?.status === 'moderation');

        // Список в кабинете отрисован на сервере и закэширован: без
        // сброса продавец вернулся бы к прежнему бейджу статуса.
        await revalidateMyListings();
      } else {
        const { error: createError } = await supabase.rpc(
          'create_car_v3',
          payload,
        );
        if (createError) throw new Error(supabaseErrorText(createError));

        // Целевое действие сайта: объявление создано и ушло на
        // модерацию. Число фотографий — полезный признак качества
        // подачи, по нему видно, доходят ли продавцы до шага с фото.
        trackEvent('listing_submitted', {
          listing_type: listingType,
          photos: photoUrls.length,
        });
      }

      // ЗАПОМИНАЕМ ТЕЛЕФОН В ПРОФИЛЕ.
      //
      // Номер спрашивается ОДИН РАЗ — при первой подаче, — а дальше
      // подставляется в форму сам (get_profile_phone при загрузке).
      // Без этой записи почтовый аккаунт заново вводил бы номер на
      // каждое объявление: session.user.phone у него пуст всегда.
      //
      // Пишем ПОСЛЕ успешной публикации, а не до: если создание
      // объявления упало, запоминать нечего — продавец, возможно, как
      // раз правит опечатку в номере.
      //
      // Ошибку глушим намеренно. Объявление уже создано и это главное;
      // несохранённый номер означает лишь, что в следующий раз поле
      // будет пустым, и рушить из-за этого успешный сценарий нельзя.
      // ЯЗЫК ПРОДАВЦА — В ПРОФИЛЬ (миграция 0121). На нём придут письмо
      // о результате модерации и причина отклонения. Здесь это особенно
      // важно: гость, подающий первое объявление, создаёт аккаунт самой
      // подачей и в AuthGate не заходит вовсе — без этой записи он
      // получил бы сербское письмо, заполнив форму по-русски.
      //
      // Ошибку глушим по той же причине, что и у телефона ниже:
      // объявление уже создано, и рушить успешный сценарий из-за
      // языка писем нельзя.
      void supabase
        .rpc('set_profile_locale', { p_locale: locale })
        .then(undefined, () => undefined);

      if (contactPhone) {
        await supabase
          .rpc('set_profile_phone', { p_phone: contactPhone })
          .then(undefined, () => undefined);
      }

      setDone(true);
    } catch (e) {
      // В консоль — только шаг и текст ошибки. Ни uid, ни номер телефона
      // сюда не пишутся: консоль браузера доступна расширениям, а это
      // персональные данные продавца.
      //
      // Текст собирает supabaseErrorText: он никогда не возвращает
      // пустую строку. Раньше здесь стоял `e.message`, и у ошибок
      // Supabase с пустым message в консоль уходило `{stage, message:''}`
      // — запись, по которой нельзя понять ни причину, ни даже то, что
      // ошибка вообще произошла.
      //
      // Шаг выводим отдельным аргументом строкой: свёрнутый объект в
      // консоли пришлось бы разворачивать, чтобы увидеть хоть что-то.
      console.error(
        `[RS Auto] Ошибка публикации объявления (шаг: ${stage})`,
        supabaseErrorText(e),
      );

      setError(humanOtpError(e, t));
    } finally {
      setBusy(false);
    }
  }

  // ---------- Загрузка объявления в режиме правки ----------
  // Скелет вместо пустой карточки: форма из десятка полей появляется
  // не мгновенно, и подсказка о том, что данные едут, избавляет от
  // ощущения сломанной страницы.
  if (loading) {
    return (
      <Card>
        <p className="text-center text-neutral-60">{t('edit_loading')}</p>
        <div className="mt-4 space-y-3">
          <SkeletonBox className="h-11 w-full" />
          <SkeletonBox className="h-11 w-full" />
          <SkeletonBox className="h-11 w-2/3" />
        </div>
      </Card>
    );
  }

  // ---------- Экран успеха ----------
  if (done) {
    // После ПРАВКИ возвращаем в кабинет: продавец пришёл оттуда, туда
    // же ему и нужно — увидеть объявление в списке с новым статусом.
    // Текст зависит от того, ушло ли объявление на повторную проверку:
    // это решил сервер, сравнив контент (миграция 0067), и сообщить об
    // этом честно важнее, чем показать одинаковое «Сохранено».
    if (isEdit) {
      return (
        <Card padding="none" className="p-6 text-center">
          <h2 className="text-h3 font-semibold">
            {movedToModeration
              ? t('edit_done_moderation_title')
              : t('edit_done_title')}
          </h2>
          <p className="mx-auto mt-2 max-w-md text-neutral-60">
            {movedToModeration
              ? t('edit_done_moderation_text')
              : t('edit_done_text')}
          </p>

          {/* Во всю ширину до sm — как в карточках «Не нашли ответ» на
              /faq, /how-it-works и /about. Кнопка по содержимому на
              узком экране висит коротким прямоугольником посреди
              карточки, и экран выглядит незаконченным. */}
          <div className="mt-6">
            <Button
              size="lg"
              href={localeHref(locale, '/my')}
              className="w-full sm:w-auto"
            >
              {t('edit_back_to_list')}
            </Button>
          </div>
        </Card>
      );
    }

    return (
      // После ПОДАЧИ ведём в каталог: объявление ещё на модерации и в
      // кабинете показать нечего, кроме бейджа «На проверке», а вот
      // посмотреть площадку в этот момент самое время.
      <Card padding="none" className="p-6 text-center">
        <h2 className="text-h3 font-semibold">{t('sell_success_title')}</h2>
        <p className="mx-auto mt-2 max-w-md text-neutral-60">
          {t('sell_success_text')}
        </p>

        {/* grid, а НЕ inline-grid: тот сжимается по содержимому, и
            [&>*]:w-full растягивал кнопки лишь до ширины самой длинной
            подписи, а не до карточки. Блочный grid занимает всю ширину,
            и на узком экране кнопки доходят до её краёв. */}
        <div className="mt-6 grid grid-cols-1 gap-3 [&>*]:w-full sm:[&>*]:w-auto sm:flex sm:flex-row sm:flex-wrap sm:items-center sm:justify-center">
          <Button size="lg" href={localeHref(locale, '/cars')}>
            {t('nf_catalog')}
          </Button>
          <Button
            variant="secondary"
            size="lg"
            href={localeHref(locale, '/my')}
          >
            {t('edit_back_to_list')}
          </Button>
        </div>
      </Card>
    );
  }

  // Год проверяется по диапазону (1900 … следующий год) — теми же
  // границами, что constraint chk_year в БД. Раньше достаточно было
  // любого непустого значения, и «12» проходило дальше, а объявление
  // отклонялось уже сервером после отправки SMS.
  const canNext1 =
    brand.trim() &&
    model.trim() &&
    validateYear(year, YEAR_MIN, yearMax()) &&
    city.trim();
  // Вошедшему продавцу код не нужен — публиковать можно сразу.
  // Гостю по-прежнему нужен отправленный и введённый код.
  const canSubmit = signedIn || (codeSent && code.trim().length >= 4);

  // Проверка шага «Детали». Дублирует серверную валидацию create_car_v3
  // намеренно: сервер — источник истины, но сообщить об ошибке до
  // загрузки фотографий и отправки SMS гораздо дешевле для пользователя.
  function validateDetails(): string | null {
    const needsRent = listingType === 'rent';

    // Сравнения идут через parseThousands, а не через Number: пустое
    // поле обязано отличаться от нуля. Number('') даёт 0, и проверка
    // «> 0» приняла бы незаполненную цену аренды за нулевую.
    if (needsRent) {
      const rent = parseThousands(rentPrice);
      if (rent === null) return t('sell_err_rent_price');
      if (rent <= 0) return t('sell_err_price_positive');
    }

    // Цена продажи может отсутствовать («Договорная»), но если указана —
    // должна быть в допустимых границах. Отрицательной она быть не может
    // в принципе: поле принимает только цифры.
    if (listingType === 'sale') {
      const sale = parseThousands(price);
      if (sale !== null && (sale < PRICE_MIN || sale > PRICE_MAX)) {
        return t('sell_err_price_range');
      }
    }

    // Обязательные поля шага. Возвращаем ПЕРВУЮ ошибку сверху вниз, в
    // порядке полей на экране: подсвечены будут все незаполненные
    // (fieldErrors ниже), а общая плашка называет ту, до которой глаз
    // дойдёт первой.
    if (fieldErrors.mileage) return t(fieldErrors.mileage);
    if (fieldErrors.bodyType) return t(fieldErrors.bodyType);
    if (fieldErrors.transmission) return t(fieldErrors.transmission);
    if (fieldErrors.fuel) return t(fieldErrors.fuel);
    if (fieldErrors.engineVolume) return t(fieldErrors.engineVolume);

    // ОПИСАНИЕ ЦЕЛИКОМ: длина, ссылки, разметка, контакты. Проверка
    // стоит здесь, на переходе со второго шага, а не в момент отправки:
    // дальше идут загрузка фотографий и (для гостя) SMS-код, и узнавать
    // о запрете после них — самое дорогое, что можно предложить
    // продавцу.
    //
    // Сервер проверит это ещё раз и откажет жёстко (миграции 0135 и
    // 0136): здесь мы экономим время, а не обеспечиваем правило.
    if (descriptionProblem) return t(descriptionProblem);

    return null;
  }

  // Переход с шага фотографий на шаг контактов.
  function goToContacts() {
    if (files.length === 0) {
      setError(t('sell_err_photos_required'));
      return;
    }
    setError(null);
    setStep(4);
  }

  // Переход со второго шага с проверкой.
  function goToPhotos() {
    // Поднимаем флаг ДО проверки: даже если человек уйдёт дальше,
    // вернувшись на шаг он увидит подсветку незаполненного.
    setDetailsTouched(true);

    const problem = validateDetails();
    if (problem) {
      setError(problem);
      return;
    }
    setError(null);
    setStep(3);
  }

  return (
    <Card>
      {/* Шапка формы: счётчик шагов слева, выход справа.
          Крестик нужен именно здесь: подача — длинная форма на
          отдельной странице, и до появления кнопки единственным
          способом «передумать» была кнопка «Назад» браузера, которая
          на первом шаге уводила вообще с сайта. Уход ведёт в каталог,
          а не history.back(): на /sell часто приходят по прямой ссылке
          из шапки, и возвращать человека в пустую историю незачем.
          Ничего не отправлено — закрытие проходит без последствий. */}
      <div className="mb-4 flex items-start justify-between gap-2">
        <div className="text-caption text-neutral-50">
          {/* В режиме правки шага входа нет — всего три. */}
          {t('sell_step')} {step} / {isEdit ? 3 : 4}
        </div>
        <CloseButton
          // При правке закрытие возвращает в кабинет, откуда продавец
          // пришёл; при подаче — в каталог: на /sell часто попадают по
          // прямой ссылке из шапки, и возвращать в пустую историю
          // незачем.
          onClick={() =>
            router.push(localeHref(locale, isEdit ? '/my' : '/cars'))
          }
          label={t('common_close')}
          // Отрицательные отступы возвращают знак к краю карточки:
          // у кнопки область 40px ради попадания пальцем, и без сдвига
          // она визуально отступала бы от угла сильнее заголовка.
          className="-mr-2 -mt-2 shrink-0"
        />
      </div>

      {/* ---------- Шаг 1: автомобиль ---------- */}
      {step === 1 && (
        <div className="space-y-3">
          <h2 className="text-h4 font-semibold">{t('sell_step_car')}</h2>

          {/* Тип объявления — первый вопрос: от него зависит, какие поля
              цен появятся на следующем шаге. */}
          <div>
            <label className="mb-1 block text-caption text-neutral-60">
              {t('sell_type')}
            </label>
            {/* Два варианта вместо трёх: у объявления один тип сделки.
                Машину, которую продают и сдают, подают двумя объявлениями. */}
            <div className="grid grid-cols-2 gap-2">
              {(
                [
                  ['sale', t('sell_type_sale')],
                  ['rent', t('sell_type_rent')],
                ] as const
              ).map(([value, label]) => (
                <button
                  key={value}
                  type="button"
                  onClick={() => setListingType(value)}
                  className={segmentedClass(listingType === value)}
                  aria-pressed={listingType === value}
                >
                  {label}
                </button>
              ))}
            </div>
          </div>

          {/* ДОСТУПНОСТЬ — ТОЛЬКО САЛОНУ И ТОЛЬКО ДЛЯ ПРОДАЖИ.
              ------------------------------------------------------------
              У аренды смысла нет: сдают ту машину, которая есть, а не
              ту, которую привезут. isDealer === null означает «ещё не
              выяснили» — до ответа поле не рисуем, чтобы оно не
              мелькнуло и не исчезло.

              РЯД ИЗ ТРЁХ КНОПОК, А НЕ ДВА ЧЕКБОКСА. Значения
              взаимоисключающи, и выбор одного из трёх физически не
              даёт отметить «под заказ» и «в пути» одновременно — то
              же правило, что в базе, где это enum. Двумя чекбоксами
              пришлось бы гасить один при нажатии другого и объяснять
              человеку, почему галочка снялась сама.

              Вёрстка та же, что у переключателя «Продаю / Сдаю» выше:
              продавец уже видел этот рисунок строкой раньше и
              понимает, что выбрать можно одно. */}
          {isDealer && listingType === 'sale' && (
            <div>
              <label className="mb-1 block text-caption text-neutral-60">
                {t('sell_availability')}
              </label>

              <div className="grid grid-cols-3 gap-2">
                {(
                  [
                    ['in_stock', t('availability_in_stock')],
                    ['on_order', t('availability_on_order')],
                    ['in_transit', t('availability_in_transit')],
                  ] as const
                ).map(([value, label]) => (
                  <button
                    key={value}
                    type="button"
                    onClick={() => setAvailability(value)}
                    className={segmentedClass(availability === value)}
                    aria-pressed={availability === value}
                  >
                    {label}
                  </button>
                ))}
              </div>

              <p className="mt-1 text-small text-neutral-50">
                {t('sell_availability_hint')}
              </p>
            </div>
          )}

          {/* СОСТОЯНИЕ АВТОМОБИЛЯ (0138).
              ------------------------------------------------------------
              БЕЗ ГЕЙТА isDealer, в отличие от доступности выше: битую
              машину, донора на разборку или машину без документов
              продаёт прежде всего частник, и прятать от него это поле
              значило бы заставлять писать «БИТАЯ!!!» в описании
              капслоком — то есть терять фильтруемые данные.

              У салона оба селектора стоят друг под другом, и это
              осознанно: оси независимы, «в пути» + «битый» —
              допустимое и вполне обычное сочетание.

              РАСКЛАДКА, А НЕ ТРИ КОЛОНКИ КАК У ДОСТУПНОСТИ: вариантов
              шесть, и подписи у них длинные («Тотал (не
              восстанавливается)»). В три колонки на 360px такая
              подпись переносится в четыре строки и кнопки разъезжаются
              по высоте. Поэтому одна колонка на мобильном и две от sm:
              строка на всю ширину читается как список, а не как сетка
              обрубков.

              ЗНАЧОК В КАЖДОМ ВАРИАНТЕ — тот же, что потом появится на
              бейдже в каталоге, И В СВОЁМ ЦВЕТЕ: оранжевый молоток у
              битого, красный лист у «без документов». Продавец выбирает
              пометку и сразу видит, как она будет выглядеть покупателю.

              ВЫБРАННЫЙ ВАРИАНТ — АКЦЕНТНОЙ РАМКОЙ, а не заливкой
              своего цвета (см. ui/segmented.ts). Красить сам вариант в
              цвет состояния заманчиво, но тогда «выбрано» и «вот такой
              будет бейдж» говорятся одним и тем же средством, и на
              сетке вариантов шаг формы превращается в светофор. Цвет
              состояния несёт значок — этого хватает, чтобы связь
              читалась, — а рамка отвечает только за выбор.

              ПУНКТА «ОБЫЧНОЕ АВТО» В СПИСКЕ НЕТ (см.
              SELECTABLE_CONDITIONS в lib/types). Ничего не выбрано —
              значит машина обычная: это и есть значение по умолчанию,
              и отдельная кнопка под него была бы кнопкой «оставить как
              есть», занимающей первое, самое заметное место.

              ОТСЮДА ПОВТОРНОЕ НАЖАТИЕ КАК СПОСОБ СНЯТЬ ПОМЕТКУ. Без
              пункта «Обычное авто» вернуться в исходное состояние
              иначе нечем: продавец, отметивший «битый» по ошибке,
              оказался бы заперт — на подаче помогла бы перезагрузка
              страницы, а вот в форме ПРАВКИ уже опубликованного
              объявления снять пометку стало бы невозможно вовсе.
              Поэтому клик по выбранному варианту возвращает 'normal'.
              aria-pressed сообщает это состояние скринридеру, а
              подпись под сеткой — глазами. */}
          <div>
            <label className="mb-1 block text-caption text-neutral-60">
              {t('sell_condition')}
            </label>

            <div className="grid grid-cols-1 gap-2 sm:grid-cols-2">
              {SELECTABLE_CONDITIONS.map((option) => {
                const active = condition === option.key;

                return (
                  <button
                    key={option.key}
                    type="button"
                    // Повторный клик по выбранному — снятие пометки.
                    onClick={() =>
                      setCondition(active ? 'normal' : option.key)
                    }
                    aria-pressed={active}
                    className={`flex items-center gap-2 text-left ${segmentedClass(
                      active,
                    )}`}
                  >
                    <ConditionIcon
                      condition={option.key}
                      className={`h-4 w-4 shrink-0 ${option.icon}`}
                    />
                    <span className="min-w-0">
                      {t(`condition_${option.key}` as DictKey)}
                    </span>
                  </button>
                );
              })}
            </div>

            <p className="mt-1 text-small text-neutral-50">
              {t('sell_condition_hint')}
            </p>
          </div>

          {/* Марка и модель — пара связанных полей, на десктопе стоят
              в одной строке: модель каскадом зависит от марки, и видеть
              их рядом естественнее, чем одно под другим.
              На мобильном (до lg) остаются в столбец — пикер со списком
              в половину узкого экрана нечитаем. */}
          <div className="grid gap-3 lg:grid-cols-2">
          <ListPicker
            locale={locale}
            name="brand"
            placeholder={t('picker_choose')}
            label={t('filter_brand')}
            options={BRANDS.map((b): PickerOption => ({ value: b, label: b }))}
            value={brand}
            allowCustom
            onChange={handleBrandChange}
          />

          {/* Модель — каскадом от марки, как в приложении. */}
          <ListPicker
            locale={locale}
            name="model"
            placeholder={t('picker_choose')}
            label={t('filter_model')}
            options={modelList.map(
              (m): PickerOption => ({ value: m.name, label: m.name }),
            )}
            value={model}
            disabled={!brand || loadingModels}
            emptyHint={
              !brand
                ? t('picker_model_no_brand')
                : loadingModels
                  ? t('picker_search')
                  : t('picker_model_empty')
            }
            allowCustom
            onChange={setModel}
          />
          </div>

          <div className="grid grid-cols-2 gap-3">
            {/* Год — ВЫБОР ИЗ СПИСКА, как в приложении
                (create_car_screen.dart): свежие годы сверху, диапазон
                тот же 1900…текущий+1, что и constraint chk_year в БД.
                Ручной ввод здесь ничего не давал: значение всё равно
                обязано попасть в этот диапазон, а опечатку вроде «20222»
                приходилось ловить проверкой уже после ввода.
                Поиск в списке оставлен: годов больше сотни, и мотать
                до 1998-го колесом дольше, чем набрать его. */}
            <ListPicker
              locale={locale}
              name="year"
              placeholder={t('picker_choose')}
              label={t('filter_year')}
              options={YEARS.map((y): PickerOption => ({ value: y, label: y }))}
              value={year}
              onChange={setYear}
            />
            {/* Город — тот же список, что в онбординге приложения.
                allowCustom оставлен, как в приложении: продавец может
                быть из города, которого нет в списке 18 крупных. */}
            <ListPicker
              locale={locale}
              name="city"
              placeholder={t('picker_choose')}
              label={t('filter_city')}
              options={CITIES.map((c): PickerOption => ({ value: c, label: c }))}
              value={city}
              allowCustom
              onChange={setCity}
            />
          </div>

          {/* Предупреждение о своём же объявлении об этой машине.
              Не Alert: тот рендерит <p>, а сюда нужны ссылка и кнопка
              внутри блока — вложить их в абзац нельзя.
              Тон предупреждения (жёлтый) взят у Alert tone="warning":
              это не ошибка, подача продолжается. */}
          {duplicate && !dupDismissed && (
            <div
              role="status"
              className="rounded-control bg-status-warning px-3 py-2.5 text-caption text-brand-gold"
            >
              <p className="font-semibold">{t('sell_dup_title')}</p>
              <p className="mt-0.5">
                {duplicate.brand} {duplicate.model} {duplicate.year}
                {/* Статус уточняем только для объявления на проверке:
                    активное продавец и так видит в каталоге, а вот
                    «оно ещё на модерации» объясняет, почему его нет
                    в выдаче, и снимает повод подавать заново. */}
                {duplicate.status === 'moderation' &&
                  ` — ${t('sell_dup_moderation')}`}
              </p>

              <div className="mt-2 flex flex-wrap items-center gap-x-4 gap-y-1">
                {/* Ссылка на карточку. site_url приходит с сервера
                    готовым (f_car_site_url) — на сербском зеркале.
                    Для /ru собирать путь заново незачем: localeHref
                    даёт адрес нужной локали по id. */}
                <Link
                  href={localeHref(locale, `/car/${duplicate.car_id}`)}
                  className="font-semibold underline underline-offset-2"
                >
                  {t('sell_dup_open')}
                </Link>

                {/* Скрыть предупреждение и продолжить. Именно кнопка,
                    а не крестик: действие нужно назвать словами —
                    продавец должен понимать, что подаёт ВТОРОЕ
                    объявление, а не закрывает случайную плашку. */}
                <button
                  type="button"
                  onClick={() => setDupDismissed(true)}
                  className="underline underline-offset-2"
                >
                  {t('sell_dup_ignore')}
                </button>
              </div>
            </div>
          )}

          <Button disabled={!canNext1} onClick={() => setStep(2)} fullWidth>
            {t('sell_next')}
          </Button>
        </div>
      )}

      {/* ---------- Шаг 2: детали ---------- */}
      {step === 2 && (
        <div className="space-y-3">
          <h2 className="text-h4 font-semibold">{t('sell_step_details')}</h2>

          {/* Цена продажи и пробег — два коротких поля в одной
              строке на всех ширинах: по отдельности они занимали бы
              два ряда без пользы. Раскладка та же, что у пар ниже
              (кузов/коробка, топливо/объём), — шаг формы читается
              единой сеткой, а не чередованием одиночных и парных
              строк. Пустая цена допустима: это «Договорная». */}
          {listingType === 'sale' && (
            <div className="grid grid-cols-2 gap-3">
              <div>
                <label
                  className="mb-1 block text-caption text-neutral-60"
                  htmlFor="sell-price"
                >
                  {t('sell_sale_price')}, €
                </label>
                <NumberInput
                  id="sell-price"
                  value={price}
                  onChange={setPrice}
                  maxDigits={PRICE_DIGITS}
                  placeholder={t('sell_price_negotiable_ph')}
                  className={field}
                />
              </div>
              <div>
                <label
                  className="mb-1 block text-caption text-neutral-60"
                  htmlFor="sell-mileage"
                >
                  {t('car_mileage')}, {t('common_km')} *
                </label>
                <NumberInput
                  id="sell-mileage"
                  value={mileage}
                  onChange={setMileage}
                  maxDigits={MAX_MILEAGE_DIGITS}
                  required
                  className={`${field}${errorFor('mileage') ? ' border-error' : ''}`}
                  aria-describedby={
                    errorFor('mileage') ? 'sell-error' : undefined
                  }
                />
              </div>
            </div>
          )}

          {/* Цена аренды и залог — только когда объявление сдаётся.
              Суточная ставка обязательна: без неё объявление аренды
              бессмысленно, и того же требует constraint в БД. */}
          {listingType === 'rent' && (
            <div className="grid grid-cols-2 gap-3">
              <div>
                <label
                  className="mb-1 block text-caption text-neutral-60"
                  htmlFor="sell-rent-price"
                >
                  {t('sell_rent_price')}, € *
                </label>
                <NumberInput
                  id="sell-rent-price"
                  value={rentPrice}
                  onChange={setRentPrice}
                  maxDigits={MAX_PRICE_DIGITS}
                  required
                  className={field}
                />
              </div>
              <div>
                <label
                  className="mb-1 block text-caption text-neutral-60"
                  htmlFor="sell-deposit"
                >
                  {t('sell_deposit')}, €
                </label>
                <NumberInput
                  id="sell-deposit"
                  value={deposit}
                  onChange={setDeposit}
                  maxDigits={MAX_PRICE_DIGITS}
                  placeholder="0"
                  className={field}
                />
              </div>
            </div>
          )}

          {/* В аренде строку выше занимают ставка и залог, поэтому
              пробег идёт отдельным полем в половину ширины. */}
          {listingType === 'rent' && (
            <div className="lg:w-1/2 lg:pr-1.5">
              <label
                className="mb-1 block text-caption text-neutral-60"
                htmlFor="sell-mileage-rent"
              >
                {t('car_mileage')}, {t('common_km')} *
              </label>
              <NumberInput
                id="sell-mileage-rent"
                value={mileage}
                onChange={setMileage}
                maxDigits={MAX_MILEAGE_DIGITS}
                required
                className={`${field}${errorFor('mileage') ? ' border-error' : ''}`}
                aria-describedby={
                  errorFor('mileage') ? 'sell-error' : undefined
                }
              />
            </div>
          )}

          {/* Кузов, коробка, топливо и объём — те же полные enum, что
              в приложении. Поиск не нужен: пунктов не больше десяти.
              Сетка 2×2 на всех ширинах, как в панели фильтров: втроём
              в ряд подписи и выбранные значения обрезались, а
              столбиком четыре поля растягивали шаг формы. */}
          <div className="grid grid-cols-2 gap-3">
            <ListPicker
              locale={locale}
              name="body_type"
              placeholder={t('picker_choose')}
              label={`${t('filter_body')} *`}
              options={Object.entries(BODY_TYPES).map(
                ([key, labels]): PickerOption => ({
                  value: key,
                  label: labels[locale],
                }),
              )}
              value={bodyType}
              searchable={false}
              onChange={setBodyType}
              invalid={Boolean(errorFor('bodyType'))}
              describedBy={errorFor('bodyType') ? 'sell-error' : undefined}
            />

            <ListPicker
              locale={locale}
              name="transmission"
              placeholder={t('picker_choose')}
              label={`${t('filter_transmission')} *`}
              options={Object.entries(TRANSMISSIONS).map(
                ([key, labels]): PickerOption => ({
                  value: key,
                  label: labels[locale],
                }),
              )}
              value={transmission}
              searchable={false}
              onChange={setTransmission}
              invalid={Boolean(errorFor('transmission'))}
              describedBy={
                errorFor('transmission') ? 'sell-error' : undefined
              }
            />

            <ListPicker
              locale={locale}
              name="fuel"
              placeholder={t('picker_choose')}
              label={`${t('filter_fuel')} *`}
              options={Object.entries(FUELS).map(
                ([key, labels]): PickerOption => ({
                  value: key,
                  label: labels[locale],
                }),
              )}
              value={fuel}
              searchable={false}
              onChange={setFuel}
              invalid={Boolean(errorFor('fuel'))}
              describedBy={errorFor('fuel') ? 'sell-error' : undefined}
            />

            {/* Объём необязателен: у электромобиля ДВС нет, и
                подсказка говорит об этом прямо, чтобы продавец не
                искал, что вписать.

                Подсказка стоит ОТДЕЛЬНЫМ абзацем, а не через
                emptyHint пикера: тот показывается только у
                заблокированного поля (как «Выберите марку» у
                модели), а объём доступен всегда — и текст не
                появился бы никогда. */}
            <div>
              <ListPicker
                locale={locale}
                name="engine_volume"
                placeholder={t('picker_choose')}
                label={t('sell_engine')}
                options={ENGINE_VOLUME_VALUES.map(
                  (v): PickerOption => ({ value: v, label: v }),
                )}
                value={engineVolume}
                onChange={setEngineVolume}
                invalid={Boolean(errorFor('engineVolume'))}
                describedBy={
                  errorFor('engineVolume') ? 'sell-error' : undefined
                }
              />
              <p className="mt-1 text-small text-neutral-50">
                {t('sell_engine_hint')}
              </p>
            </div>
          </div>

          <div>
            <label className="mb-1 block text-caption text-neutral-60">
              {t('car_description')} *
            </label>
            <textarea
              value={description}
              onChange={(e) => setDescription(e.target.value)}
              rows={5}
              // maxLength обрезает ввод на верхней границе, но правилом
              // является констрейнт базы (0136): поле можно обойти.
              maxLength={DESCRIPTION_MAX}
              placeholder={t('car_description_hint')}
              // Рамка краснеет сразу, как только в тексте появился
              // номер, ссылка или тег: продавец видит связь между тем,
              // что печатает, и отказом, — а не узнаёт о запрете на
              // последнем шаге.
              //
              // Короткое описание рамку НЕ красит: пока человек
              // печатает, оно короткое по определению, и краснеть с
              // первого символа значит ругаться на процесс письма.
              // Про минимум сообщает счётчик и проверка при переходе.
              className={`${fieldTextarea}${
                hasDescriptionContacts ||
                descriptionProblem === 'sell_err_desc_links' ||
                descriptionProblem === 'sell_err_desc_html' ||
                // Пустое обязательное поле — но только после попытки
                // уйти дальше, как и у остальных полей шага.
                (detailsTouched &&
                  descriptionProblem === 'sell_err_desc_required')
                  ? ' border-error'
                  : ''
              }`}
              aria-invalid={descriptionProblem !== null || undefined}
              aria-describedby={
                descriptionProblem ? 'description-problem' : undefined
              }
            />

            {/* СЧЁТЧИК. Показывает длину без крайних пробелов — ту же,
                что проверяют валидатор и база. До минимума показываем
                «сколько ещё нужно»: это подсказка, а не упрёк. */}
            <div className="mt-1 flex items-start justify-between gap-3">
              <p className="text-small text-neutral-50">
                {/* Только про НАЧАТОЕ, но короткое описание: человек уже
                    пишет, и порог ему полезен прямо сейчас.

                    Про ПУСТОЕ здесь молчим намеренно: та же просьба
                    заполнить показывается над кнопкой «Далее», и рядом
                    со счётчиком «0 / 6000» она была третьим повтором
                    одной фразы на одном экране. */}
                {descriptionLen > 0 && descriptionLen < DESCRIPTION_MIN
                  ? t('sell_err_desc_short')
                  : ''}
              </p>
              {/* Счётчик — нейтральный факт, а не приговор, поэтому
                  цвет обычный и не меняется. Красным он дублировал
                  ошибку, которая и так сказана словами над кнопкой
                  «Далее», и на пустом поле «0 / 6000» выглядел
                  обвинением там, где человек ещё ничего не сделал. */}
              <p className="shrink-0 text-small tabular-nums text-neutral-60">
                {descriptionLen} / {DESCRIPTION_MAX} {t('sell_desc_counter')}
              </p>
            </div>

            {/* ПОДСВЕТКА НАЙДЕННОГО.
                Textarea не умеет красить куски своего текста, а
                накладывать поверх него позиционированный слой — приём
                хрупкий: он ломается на переносах строк, скролле и
                разных шрифтах. Поэтому показываем найденные фрагменты
                ОТДЕЛЬНЫМ списком под полем. Продавцу нужно понять, что
                именно убрать, и точная позиция в тексте для этого не
                обязательна — достаточно увидеть сам номер. */}
            {/* Блок ПОДСВЕТКИ — только для находок в тексте: контактов,
                ссылок, разметки. Условие проверяет наличие самих
                совпадений, а не «есть какая-то ошибка»: раньше здесь
                стоял список исключений (short, long), и с появлением
                нового ключа required пустое описание провалилось сюда —
                показывались заголовок «Найдено в описании:» без единой
                находки и дубль того же сообщения, что и внизу формы.
                Проверка по совпадениям такого повторить не может. */}
            {hasDescriptionContacts && (
              <div id="description-problem" className="mt-2">
                {/* Плашки с текстом ошибки здесь НЕТ намеренно: та же
                    фраза уже показана над кнопкой «Далее». Дублировать
                    её рядом с полем значит трижды написать одно и то же
                    на одном экране — ровно это и получалось раньше.

                    Здесь остаётся только подсветка: она показывает не
                    «что не так», а ГДЕ именно, и этого рядом с полем
                    не заменить ничем.

                    Заголовок зависит от причины: при ссылке или
                    разметке говорить «контактные данные» было бы
                    неверно. */}
                <p className="text-caption text-neutral-60">
                  {descriptionProblem === 'sell_err_contacts'
                    ? t('sell_contacts_found')
                    : t('sell_desc_found')}
                </p>
                <p className="mt-1 break-words text-caption">
                  {/* Слитный текст описания с покрашенными находками:
                      так видно и сам фрагмент, и его окружение. */}
                  {splitByContacts(description).map((chunk, i) =>
                    chunk.match ? (
                      <mark
                        key={i}
                        className="rounded-control bg-status-error px-0.5 text-brand-red"
                      >
                        {chunk.text}
                      </mark>
                    ) : (
                      // Чистые куски показываем приглушённо: внимание
                      // должно уходить на подсвеченное, а не на весь
                      // текст целиком.
                      <span key={i} className="text-neutral-50">
                        {chunk.text}
                      </span>
                    ),
                  )}
                </p>
              </div>
            )}
          </div>

          {/* ОШИБКА ШАГА — НАД КНОПКАМИ, а не в общей плашке внизу
              карточки. Причина в расстоянии: на длинном шаге «Детали»
              нижняя плашка оказывалась под кнопками и ниже сгиба, и
              человек нажимал «Далее» второй раз, не увидев, что ему
              уже ответили.

              Перед ВСЕМ рядом, а не перед кнопкой: на мобильном ряд
              идёт flex-col-reverse, и «Далее» там первая сверху —
              вставка между кнопками оказалась бы под ней. */}
          {error && (
            <Alert id="sell-error" tone="error">
              {error}
            </Alert>
          )}

          {/* Та же раскладка, что на шаге фотографий: на мобильном
              столбец с «Далее» сверху, с sm — ряд. Подробно см.
              комментарий там. */}
          <div className="flex flex-col-reverse gap-3 sm:flex-row">
            <Button
              variant="secondary"
              onClick={() => setStep(1)}
              fullWidth
              className="sm:w-auto"
            >
              {t('sell_back')}
            </Button>
            {/* flex-1: «Далее» занимает всё оставшееся место — главное
                действие шага должно быть заметно шире «Назад». */}
            <Button onClick={goToPhotos} className="flex-1">
              {t('sell_next')}
            </Button>
          </div>
        </div>
      )}

      {/* ---------- Шаг 3: фотографии ---------- */}
      {step === 3 && (
        <div className="space-y-3">
          <h2 className="text-h4 font-semibold">{t('sell_step_photos')}</h2>

          <PhotoPicker
            locale={locale}
            files={files}
            onChange={setFiles}
            maxPhotos={MAX_PHOTOS}
          />

          {/* ОШИБКА ШАГА — НАД КНОПКАМИ, как на шаге «Детали».
              Причина та же: снизу, под кнопками, сообщение оказывается
              ниже сгиба, и «Далее» жмут второй раз, не увидев ответа.
              Перед ВСЕМ рядом, а не между кнопками: ряд идёт
              flex-col-reverse, и на мобильном «Далее» там первая
              сверху. */}
          {error && (
            <Alert id="sell-error" tone="error">
              {error}
            </Alert>
          )}

          {/* НА МОБИЛЬНОМ — СТОЛБЕЦ, «Далее» СВЕРХУ.
              flex-col-reverse переворачивает порядок отрисовки, не
              трогая разметку: в DOM «Назад» остаётся первым (так его
              находит клавиатура и скринридер — сначала возврат, потом
              продолжение), а видит человек «Далее» над «Назад».
              Главное действие обязано быть выше и попадать под большой
              палец, а не прятаться под второстепенным.

              Обе кнопки во всю ширину: на узком экране две кнопки
              разной ширины друг под другом читаются как ошибка
              вёрстки. С sm возвращается прежний ряд, где «Назад»
              по содержимому, а «Далее» занимает остаток. */}
          <div className="flex flex-col-reverse gap-3 sm:flex-row">
            <Button
              variant="secondary"
              onClick={() => setStep(2)}
              fullWidth
              className="sm:w-auto"
            >
              {t('sell_back')}
            </Button>
            {/* Без единой фотографии дальше не пускаем: объявление без
                снимков в каталоге показывается серой заглушкой и почти
                не получает откликов. Проверка именно здесь, а не при
                отправке, — иначе продавец узнал бы о ней после ввода
                телефона и SMS-кода.

                В режиме правки это последний шаг: входить некуда,
                поэтому кнопка сразу сохраняет. */}
            {isEdit ? (
              <Button onClick={submit} disabled={busy} className="flex-1">
                {busy ? t('edit_saving') : t('edit_submit')}
              </Button>
            ) : (
              <Button onClick={goToContacts} className="flex-1">
                {t('sell_next')}
              </Button>
            )}
          </div>

          {/* Предупреждение о повторной модерации. Тон warning — тот же
              золотой, что у бейджа «На проверке» в кабинете: цвет
              предупреждения совпадает с цветом статуса, который
              наступит после сохранения. */}
          {isEdit && (
            <p className="rounded-control bg-warning/10 px-3 py-2 text-caption text-warning">
              {t('edit_moderation_warning')}
            </p>
          )}

          {/* Прогресс отправки новых фотографий. При правке загрузка
              начинается сразу по нажатию «Сохранить», и без индикатора
              кнопка выглядит зависшей. */}
          {isEdit && busy && files.some((item) => item.kind === 'file') && (
            <div>
              <div className="flex items-center justify-between text-caption text-neutral-60">
                <span>{t('sell_photos_uploading')}</span>
                <span>{uploadProgress}%</span>
              </div>
              <div className="mt-1.5 h-2 overflow-hidden rounded-pill bg-surface-muted">
                <div
                  className="h-full rounded-pill bg-brand-green transition-[width] duration-normal ease-out"
                  style={{ width: `${uploadProgress}%` }}
                />
              </div>
            </div>
          )}
        </div>
      )}

      {/* ---------- Шаг 4: контакты и вход по SMS ---------- */}
      {/* СВЕРКА НОМЕРА ПЕРЕД ПУБЛИКАЦИЕЙ.
          Перекрывает содержимое шага 4 целиком, а не встаёт рядом:
          вопрос требует ответа, и оставлять под ним активную форму
          значило бы позволить опубликовать в обход проверки.

          Номер показан крупно (text-h4) и в том же виде, в каком его
          увидит покупатель, — сверять мелкий серый текст бесполезно.

          Две кнопки, и «Изменить» стоит первой: она возвращает к форме,
          то есть отменяет действие, а отмена по правилам площадки
          всегда левее подтверждения. */}
      {phoneToConfirm !== null && (
        <div className="space-y-3">
          <h2 className="text-h4 font-semibold">
            {t('sell_phone_confirm_title')}
          </h2>
          <p className="text-caption text-neutral-60">
            {t('sell_phone_confirm_text')}
          </p>

          <p className="rounded-card border border-neutral-10 bg-surface-subtle px-4 py-3 text-center text-h4 font-semibold">
            {formatSerbianPhone(phoneToConfirm)}
          </p>

          {/* size="sm" вместо размера по умолчанию: обе подписи стали
              короткими («Изменить», «Верно»), и кнопки высотой с
              «Опубликовать» смотрелись бы тяжело — это подтверждение
              одного поля, а не главное действие формы. */}
          <div className="flex gap-2">
            <Button
              variant="secondary"
              size="sm"
              onClick={() => setPhoneToConfirm(null)}
              disabled={busy}
              className="flex-1"
            >
              {t('sell_phone_confirm_edit')}
            </Button>
            <Button
              size="sm"
              onClick={() => {
                setPhoneToConfirm(null);
                void submit();
              }}
              disabled={busy}
              className="flex-1"
            >
              {t('sell_phone_confirm_ok')}
            </Button>
          </div>
        </div>
      )}

      {step === 4 && phoneToConfirm === null && (
        <div className="space-y-3">
          <h2 className="text-h4 font-semibold">{t('sell_step_contact')}</h2>

          {/* Honeypot. Скрыт от пользователя и от скринридеров, но виден
              ботам, которые заполняют все поля формы подряд. */}
          <input
            type="text"
            name="website"
            value={website}
            onChange={(e) => setWebsite(e.target.value)}
            tabIndex={-1}
            autoComplete="off"
            aria-hidden="true"
            style={{
              position: 'absolute',
              left: '-9999px',
              width: 1,
              height: 1,
              opacity: 0,
            }}
          />

          {/* Телефон спрашиваем у гостя И у вошедшего БЕЗ номера.
              Раньше условие было просто «у гостя»: единственным входом
              был SMS, и номер имелся у каждого вошедшего. С входом по
              почте (0082) появились аккаунты без телефона — и для них
              поле обязано вернуться, иначе объявление уходило бы без
              контакта, а публикация падала бы с невнятной ошибкой. */}
          {(signedIn === false || (signedIn === true && !accountPhone)) && (
            <div>
              <label className="mb-1 block text-caption text-neutral-60">
                {t('sell_phone')}
              </label>
              {/* Маска «+381 6X XXX XXX(X)» — та же, что в приложении
                  (SerbianPhoneFormatter). Форматирование идёт по мере
                  набора, наружу уходит E.164. */}
              <input
                type="tel"
                inputMode="tel"
                value={phone}
                onChange={(e) => setPhone(formatSerbianPhone(e.target.value))}
                // Каретка встаёт в конец номера, а не туда, куда попал
                // палец: в поле стоит код страны «+381 », и тап в его
                // середину уводил набранную цифру внутрь кода.
                {...phoneCaret}
                placeholder="6X XXX XXX"
                className={field}
                disabled={codeSent}
              />
            </div>
          )}

          {/* Проверка сессии ещё идёт: не показываем ничего, иначе
              у вошедшего продавца на долю секунды мелькнёт блок
              «Отправить код», которого он не должен видеть вовсе. */}
          {signedIn === null ? null : signedIn ? (
            <>
              {/* Продавец уже вошёл — публикуем без SMS. Код нужен для
                  создания сессии, а она есть и живёт между визитами
                  (persistSession). Требовать SMS на каждое объявление
                  значило бы жечь суточную квоту в пять сообщений
                  и заставлять человека ждать на ровном месте. */}
              {/* У вошедшего БЕЗ телефона кнопка ждёт валидного номера:
                  он идёт прямо в объявление, и пустой контакт сделал бы
                  его бесполезным для покупателя. У вошедшего С номером
                  проверять нечего — телефон берётся из аккаунта. */}
              <Button
                onClick={requestSubmit}
                disabled={
                  busy || (!accountPhone && !isValidSerbianContactPhone(phone))
                }
                fullWidth
              >
                {busy ? t('otp_verifying') : t('sell_submit')}
              </Button>

              {/* Прогресс отправки фотографий: у вошедшего публикация
                  начинается сразу с загрузки, и без индикатора кнопка
                  выглядит зависшей. */}
              {busy && files.length > 0 && (
                <div>
                  <div className="flex items-center justify-between text-caption text-neutral-60">
                    <span>{t('sell_photos_uploading')}</span>
                    <span>{uploadProgress}%</span>
                  </div>
                  <div className="mt-1.5 h-2 overflow-hidden rounded-pill bg-surface-muted">
                    <div
                      className="h-full rounded-pill bg-brand-green transition-[width] duration-normal ease-out"
                      style={{ width: `${uploadProgress}%` }}
                    />
                  </div>
                </div>
              )}
            </>
          ) : !codeSent ? (
            <>
              {/* ПОЧТА — способ входа для гостя.
                  Аккаунт создаётся кодом на этот адрес, поэтому поле
                  стоит рядом с телефоном, но означает совсем другое:
                  телефон выше увидит покупатель, почту не увидит
                  никто — она нужна только для входа. Подпись под полем
                  об этом и говорит: иначе продавец решит, что оставляет
                  второй публичный контакт. */}
              <div>
                <label className="mb-1 block text-caption text-neutral-60">
                  {t('auth_email_label')}
                </label>
                <input
                  type="email"
                  inputMode="email"
                  autoComplete="email"
                  value={email}
                  onChange={(e) => setEmail(e.target.value)}
                  placeholder={t('auth_email_ph')}
                  className={field}
                />
                <p className="mt-1 text-small text-neutral-50">
                  {t('sell_email_hint')}
                </p>
              </div>

              {/* Согласие с условиями и политикой — ОБЯЗАТЕЛЬНО до кнопки
                  «Получить код». Тот же порядок, что в приложении: аккаунт
                  создаётся самим входом, поэтому документы
                  принимаются здесь, а не после публикации.
                  Ссылки ведут на страницы сайта и открываются в новой
                  вкладке — иначе продавец потеряет заполненную форму. */}
              <label className="flex cursor-pointer items-start gap-2.5 text-caption text-neutral-70">
                <input
                  type="checkbox"
                  checked={agreed}
                  onChange={(e) => setAgreed(e.target.checked)}
                  className="mt-0.5 h-4 w-4 shrink-0 accent-brand-green"
                />
                <span>
                  {t('legal_consent_before')}
                  <Link
                    href={localeHref(locale, '/terms')}
                    target="_blank"
                    rel="noopener"
                    className="font-semibold text-brand-blue underline"
                  >
                    {t('legal_consent_terms')}
                  </Link>
                  {t('legal_consent_and')}
                  <Link
                    href={localeHref(locale, '/privacy')}
                    target="_blank"
                    rel="noopener"
                    className="font-semibold text-brand-blue underline"
                  >
                    {t('legal_consent_privacy')}
                  </Link>
                  .
                </span>
              </label>

              <Button
                onClick={() => sendCode()}
                // Кнопка неактивна без согласия: аккаунт создаётся
                // самим входом, и заводить его раньше принятия
                // документов нельзя.
                //
                // Проверяются ОБА поля. Телефон — потому что он уйдёт
                // в объявление контактом, и пустой сделал бы его
                // бесполезным для покупателя; проверка по существу, а
                // не на непустоту: в поле всегда стоит «+381 ».
                // Почта — потому что на неё придёт код входа;
                // достаточно «@», точную форму проверит sendCode.
                disabled={
                  busy ||
                  !isValidSerbianContactPhone(phone) ||
                  !email.includes('@') ||
                  !agreed
                }
                variant="info"
                fullWidth
              >
                {busy ? t('otp_sending') : t('sell_send_code')}
              </Button>
            </>
          ) : (
            <>
              <p className="text-caption text-neutral-60">
                {t('auth_email_sent_to')} {sentTo}
              </p>

              <div>
                <label className="mb-1 block text-caption text-neutral-60">
                  {t('sell_code')}
                </label>
                <input
                  type="text"
                  inputMode="numeric"
                  autoComplete="one-time-code"
                  maxLength={6}
                  value={code}
                  onChange={(e) =>
                    // Только цифры: в приложении поле кода тоже
                    // ограничено digitsOnly.
                    setCode(e.target.value.replace(/\D/g, ''))
                  }
                  className={field}
                />
              </div>

              {/* «Изменить номер» и «Отправить снова» — как в приложении.
                  Повторная отправка блокируется на 60 секунд: без таймера
                  продавец выжжет суточную квоту в пять SMS за минуту. */}
              <div className="flex items-center justify-between text-caption">
                <button
                  type="button"
                  onClick={changeNumber}
                  disabled={busy}
                  className="font-semibold text-brand-blue disabled:opacity-40"
                >
                  {t('auth_email_change')}
                </button>
                <button
                  type="button"
                  onClick={() => sendCode(true)}
                  disabled={busy || resendIn > 0}
                  className="font-semibold text-brand-blue disabled:opacity-40"
                >
                  {resendIn > 0
                    ? `${t('otp_resend_in')} (${resendIn})`
                    : t('otp_resend')}
                </button>
              </div>

              <Button
                onClick={requestSubmit}
                disabled={busy || !canSubmit}
                fullWidth
              >
                {busy
                  ? phoneVerified
                    ? t('sell_submit')
                    : t('otp_verifying')
                  : t('sell_submit')}
              </Button>

              {/* Прогресс отправки фотографий. Загрузка идёт ПОСЛЕ
                  подтверждения кода (нужна сессия для RLS), то есть
                  на этом шаге, а не на шаге выбора файлов. Без него
                  публикация 15 снимков выглядит как зависшая кнопка. */}
              {busy && phoneVerified && files.length > 0 && (
                <div>
                  <div className="flex items-center justify-between text-caption text-neutral-60">
                    <span>{t('sell_photos_uploading')}</span>
                    <span>{uploadProgress}%</span>
                  </div>
                  <div className="mt-1.5 h-2 overflow-hidden rounded-pill bg-surface-muted">
                    <div
                      className="h-full rounded-pill bg-brand-green transition-[width] duration-normal ease-out"
                      style={{ width: `${uploadProgress}%` }}
                    />
                  </div>
                </div>
              )}
            </>
          )}

          <Button variant="secondary" onClick={() => setStep(3)} fullWidth>
            {t('sell_back')}
          </Button>
        </div>
      )}

      {/* Успешная повторная отправка. В приложении это зелёный снек;
          на сайте — та же роль, но без всплывающего слоя. */}
      {notice && !error && (
        <Alert tone="success" className="mt-4">
          {notice}
        </Alert>
      )}

      {/* Плашка ошибки для ОСТАЛЬНЫХ шагов. Ошибки показываются в ней
          ПО ОЧЕРЕДИ, сверху вниз по порядку полей: подсвечены при этом
          все незаполненные сразу (красная граница), а текст называет
          ту, до которой глаз дойдёт первой.

          Почему не подпись под каждым полем: пикеры кузова, коробки,
          топлива и объёма лежат в сетке 2×2, и текст ошибки становился
          в ней ОТДЕЛЬНОЙ ЯЧЕЙКОЙ — поля разъезжались по колонкам, и
          вёрстка шага ломалась. Вариант с обёрткой на каждое поле
          вернул бы раскладку, но заставил бы блоки прыгать по высоте
          при появлении текста. Общая строка не двигает ничего.

          У шагов «Детали» и «Фотографии» своя плашка — прямо над
          кнопками (см. выше). Здесь они исключены, иначе одна ошибка
          печаталась бы дважды, а id="sell-error" оказался бы в
          документе в двух экземплярах и сломал бы aria-describedby у
          полей. */}
      {error && step !== 2 && step !== 3 && (
        <Alert id="sell-error" tone="error" className="mt-4">
          {error}
        </Alert>
      )}
    </Card>
  );
}
