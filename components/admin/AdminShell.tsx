// ============================================================
// RS AUTO — Каркас админ-комнаты. Server Component.
// ============================================================
// Держит раскладку и ничего больше: тонкая шапка сверху плюс рабочая
// область. Одинаково на всех вьюпортах — в этом и смысл редизайна.
//
// НАВИГАЦИЯ УБРАНА ЦЕЛИКОМ. До этого здесь был AdminSidebar: панель
// 220px слева на десктопе и прокручиваемая строка пунктов сверху на
// мобильном. Две разные раскладки одного меню, каждую нужно было
// править при добавлении раздела.
//
// Теперь модель одна на оба вьюпорта: главный экран — сетка окон,
// раздел — экран с кнопкой «← На главную». Ровно то же поведение,
// что у мобильного приложения, и ровно то же на мониторе. Разделы
// перечислены в одном месте — на главной (app/admin/page.tsx), и
// новый раздел добавляется одной карточкой там.
//
// ШАПКА И ПОДВАЛ САЙТА НЕ ИСПОЛЬЗУЮТСЯ НАМЕРЕННО. Админка — рабочий
// инструмент, а не страница продукта: переключатель языка, поиск,
// «Подать объявление» и юридические ссылки в подвале здесь только
// отнимают высоту экрана, которая нужна под очередь.
//
// Раскладка на трёх брейкпоинтах:
//   360px  — шапка в одну строку, содержимое во всю ширину,
//            отступы 16px; сетка окон в 2 колонки;
//   768px  — то же, сетка в 3 колонки;
//   1280px — содержимое ограничено 1200px по ширине, чтобы строки
//            таблиц не растягивались на весь монитор до нечитаемости;
//            сетка окон в 4 колонки.
// ============================================================

import type { ReactNode } from 'react';

import AdminHeader from '@/components/admin/AdminHeader';

export default function AdminShell({ children }: { children: ReactNode }) {
  return (
    <div className="min-h-dvh">
      <AdminHeader />

      {/* min-w-0 обязателен: без него широкая таблица распирает
          контейнер вместо того, чтобы прокручиваться внутри него. */}
      <main className="min-w-0 px-4 py-6 lg:px-8 lg:py-8">
        <div className="mx-auto max-w-[1200px]">{children}</div>
      </main>
    </div>
  );
}
