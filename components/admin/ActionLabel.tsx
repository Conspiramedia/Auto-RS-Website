// ============================================================
// RS AUTO — Подпись действия в журнале. Server Component.
// ============================================================
// Переводит код действия из admin_action_log в человеческую фразу.
// Коды пишут RPC (0078, 0080), и перечень открытый: журнал должен
// уметь показать запись, сделанную функцией, о которой этот файл ещё
// не знает.
//
// Отсюда правило: неизвестный код выводится КАК ЕСТЬ, а не
// заменяется на «неизвестное действие». Сырой код говорит хотя бы
// что-то и позволяет найти виновника поиском по кодовой базе;
// заглушка не говорит ничего.
// ============================================================

const ACTIONS: Record<string, { label: string; className: string }> = {
  car_approved: { label: 'Объявление одобрено', className: 'text-success' },
  car_rejected: { label: 'Объявление отклонено', className: 'text-error' },
  car_archived: { label: 'Снято с публикации', className: 'text-error' },
  car_restored: { label: 'Возвращено в выдачу', className: 'text-success' },

  // Возврат из архива САМИМ ВЛАДЕЛЬЦЕМ (0089). Актор в этих записях —
  // продавец, а не модератор, и цвет нейтральный: это не решение
  // администрации, а обычное действие владельца над своим объявлением.
  // В журнале оно нужно затем, чтобы модератор видел полную картину
  // жизни объявления, а не только свои собственные решения.
  car_restored_by_owner: {
    label: 'Владелец вернул из архива',
    className: 'text-neutral-70',
  },

  // Действия над салонами (0085).
  dealer_trusted_on: {
    label: 'Салон: без модерации включено',
    className: 'text-neutral-70',
  },
  dealer_trusted_off: {
    label: 'Салон: без модерации выключено',
    className: 'text-neutral-70',
  },
  dealer_blocked: { label: 'Салон заблокирован', className: 'text-error' },

  // Отзыв статуса салона (0125). Отдельно от блокировки: та скрывает
  // объявления, но продавец остаётся салоном, а здесь у него забрали
  // сам статус — витрина отключена, заявка отозвана. В ленте эти
  // решения обязаны читаться порознь.
  dealer_revoked: { label: 'Статус салона отозван', className: 'text-error' },

  // Решения по заявкам на статус салона (0100). Одобрение зелёное —
  // в отличие от тумблера доверия рядом: это не пометка, а выдача
  // прав, и в ленте журнала оно должно читаться как событие, а не
  // как настройка. Отказ красный по той же причине, что и отказ в
  // модерации: заявитель получил «нет» и увидел причину.
  dealer_app_approved: {
    label: 'Статус салона выдан',
    className: 'text-success',
  },
  dealer_app_rejected: {
    label: 'Заявка на статус салона отклонена',
    className: 'text-error',
  },

  // Автопубликация (0086). Актор в этих записях — сам салон, а не
  // администратор: решение приняла система по выданному салону праву.
  //
  // car_auto_approved НЕ красим зелёным, в отличие от ручного
  // car_approved: зелёный в журнале означает «модератор посмотрел и
  // одобрил», а здесь никто не смотрел. Нейтральный цвет честнее.
  car_auto_approved: {
    label: 'Опубликовано автоматически',
    className: 'text-neutral-70',
  },
  // Не отклонение, а уход в обычную очередь — поэтому не красный.
  // И не золотой (цвет статуса moderation на сайте): brand.gold как
  // ЦВЕТ ТЕКСТА даёт на белом 2.09:1 при норме WCAG AA 4.5:1 — на
  // заливке он работает, набранный текстом нечитаем.
  car_autopublish_skipped: {
    label: 'Автопубликация не прошла — в очередь',
    className: 'text-neutral-70',
  },
};

export function actionLabel(action: string): string {
  return ACTIONS[action]?.label ?? action;
}

export default function ActionLabel({ action }: { action: string }) {
  const known = ACTIONS[action];

  return (
    <span
      className={`whitespace-nowrap font-medium ${known?.className ?? 'text-neutral-70'}`}
    >
      {known?.label ?? action}
    </span>
  );
}
