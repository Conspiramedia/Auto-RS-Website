// ============================================================
// RS AUTO — Содержимое страницы подачи /sell, общее для sr и ru.
// ============================================================
// СПИСКА ШАГОВ ЗДЕСЬ БОЛЬШЕ НЕТ, и это осознанно. Человек, открывший
// /sell, уже решил подать объявление — ему нужна форма, а не пересказ
// процесса перед ней. Три строки «Подайте объявление / Дождитесь
// проверки / Получайте сообщения» отодвигали само действие вниз,
// ничего не сообщая: каждый шаг очевиден из самой формы.
//
// Вместе со списком убрана и разметка HowTo: Google требует, чтобы
// шаги в ней совпадали с ВИДИМЫМ текстом страницы, и разметка без
// списка стала бы ошибкой структурированных данных. Одно без другого
// на этой странице не живёт.
//
// Сценарий продавца никуда не делся — он остаётся на /how-it-works
// вместе со своей разметкой HowTo, там ему и место: это страница про
// то, как всё устроено.
// ============================================================

import SellForm from '@/components/SellForm';
import SiteFooter from '@/components/SiteFooter';
import SiteHeader from '@/components/SiteHeader';
import type { Locale } from '@/lib/i18n';
import { getT } from '@/lib/i18n';

export default function SellPageView({ locale }: { locale: Locale }) {
  const t = getT(locale);

  return (
    <>
      <SiteHeader locale={locale} pathname="/sell" />

      {/* max-w-3xl на десктопе вместо 2xl: форма подачи получила парные
          поля (марка+модель, цена+пробег), и на 672px две колонки
          становятся слишком узкими для пикеров со списком.
          Шире 768px не идём — поля ввода во всю ширину экрана читаются
          плохо, а форма остаётся линейным сценарием, а не панелью. */}
      <main className="mx-auto max-w-2xl px-4 py-6 sm:py-8 lg:max-w-3xl">
        <h1 className="text-h2 font-bold sm:text-h1">{t('sell_title')}</h1>
        <p className="mt-2 text-neutral-60">{t('sell_subtitle')}</p>

        <div className="mt-6">
          <SellForm locale={locale} />
        </div>
      </main>

      <SiteFooter locale={locale} />
    </>
  );
}
