# Чек-лист релиза RS Auto

Незакрытые **ручные и конфигурационные** задачи: то, что нельзя сделать
правкой кода — покупка домена, записи DNS, ключи провайдеров,
регистрационные данные юридического лица, вычитка юристом.

Задачи по коду здесь не дублируются: они живут в `README.md` → TODO.

**Правило:** пункт закрывается — отмечается здесь же (`[ ]` → `[x]`) с
датой и фактическим значением, если оно не секрет. Файл обновляется в
том же коммите, что и закрывающее изменение.

Статус на 23 августа 2026: **сайт функционально готов, релиз держат
пункты 1–5.**

---

## 1. Домен `rsauto.rs`

Порядок важен: пока домена нет, письма уходить не могут (Resend требует
верифицированный домен отправителя), а canonical и OG указывают на
временный адрес Vercel.

- [ ] Купить домен `rsauto.rs`.
- [ ] DNS для Vercel: A/CNAME по инструкции панели → домен привязан к
      проекту, HTTPS выпущен.
- [ ] DNS для Resend: записи **SPF** и **DKIM** из панели Resend →
      дождаться статуса «Verified». Без них письма не отправляются
      вовсе, а не «попадают в спам».
- [ ] `NEXT_PUBLIC_SITE_BASE_URL=https://rsauto.rs` в переменных Vercel
      (Production) → **redeploy** (переменная читается на сборке,
      без пересборки старый адрес останется в canonical и sitemap).
- [ ] Адрес на сервере — тем же значением, иначе `site_url` объявлений
      разойдётся с canonical:
      ```sql
      select public.set_site_base_url('https://rsauto.rs');
      ```
- [ ] `supabase secrets set MAIL_FROM=noreply@rsauto.rs`
      (в секрет кладётся **голый адрес**; отображаемое имя «RS Auto»
      функция подставляет сама — строка вида `RS Auto <…>` в этой
      переменной сломает отправку).
- [ ] `supabase secrets set RESEND_API_KEY=re_…`
- [ ] `supabase functions deploy send-email` + расписание `*/5 * * * *`
      (Dashboard → Edge Functions → Schedules).

Проверка: сменить статус тестового объявления на `active` → строка в
`email_queue` со статусом `sent` в течение 5 минут.

## 2. Почта поддержки

Решение не принято — **выбрать один вариант**:

- **А. Пересылка.** Завести ящики `support@rsauto.rs` и `info@rsauto.rs`
  на почтовом хостинге домена, настроить пересылку на
  `info.rsauto.rs@gmail.com`. Код не меняется: `admin_email` уже равен
  `support@rsauto.rs`, `OPERATOR.email` — `info@rsauto.rs`.
- **Б. Напрямую на Gmail.** Одна SQL-команда, ящиков на домене не нужно:
  ```sql
  select public.set_admin_email('info.rsauto.rs@gmail.com');
  ```
  Минус: в публичных документах и на `/contact` останется
  `info@rsauto.rs` (`lib/legal.ts`), и адрес для входящих разойдётся
  с заявленным — либо править и его.

- [ ] Вариант выбран и настроен: ______

## 3. Реквизиты оператора (APR)

Блокирует релиз: публичная оферта без регистрационных данных
неполноценна, а выдуманный номер APR — нарушение сам по себе.

- [ ] **MB** (матични број) — `lib/legal.ts` → `OPERATOR.registrationNumber`
- [ ] **PIB** (налоговый номер) — `OPERATOR.taxNumber`
- [ ] **Адрес регистрации из выписки APR** — `OPERATOR.address`.
      Именно регистрационный, не фактический адрес работы и не адрес
      для корреспонденции.
- [ ] После заполнения всех трёх `OPERATOR_VERIFIED` становится `true`
      автоматически, и блоки реквизитов появляются на `/contact` и в
      разделах «Контакты» обоих документов. Проверить обе локали.

Телефон поддержки **не заводится** — решение принято, поддержка только
по электронной почте. `OPERATOR.phone` остаётся пустым.

## 4. Юридическая вычитка

- [ ] Юрист вычитывает `privacy` и `terms` (тексты — `lib/legal.ts`,
      применимое право: Сербия, сервис 18+).
- [ ] При правке формулировок поднять `POLICY_VERSION` в **трёх**
      местах одновременно:
      - `lib/legal.ts`
      - `lib/consent.ts`
      - приложение: `lib/features/legal/policy_content.dart` →
        `kPolicyVersion`

      Расхождение версий означает, что один клиент считает согласие
      действующим, а другой — устаревшим.

## 5. Демо-данные

- [ ] Выполнить `docs/cleanup_demo.sql` в SQL Editor **до открытия
      индексации**. В базе 27 демо-объявлений из миграции `0054`; все
      принадлежат служебному продавцу
      `00000000-0000-4000-a000-0000000000de` и помечены `[DEMO]`.

Попав в индекс Google, демо-машины вычищаются из выдачи неделями —
поэтому пункт закрывается раньше, чем сайт открывается роботам.

## 6. Применить миграцию 0072

- [x] `supabase/migrations/0072_withdrawn_listing_and_dealer_list.sql`
      — выполнена в SQL Editor вручную 23 августа 2026.

Что она делает:
- `get_car_details` начинает отдавать посторонним снятые, отклонённые
  и ушедшие на перепроверку объявления **в урезанном виде** (без цен,
  описания, контактов и витрины продавца) — чтобы ссылка из выдачи
  вела на страницу «объявление снято», а не на голую 404;
- добавляет `get_site_dealers` — список салонов для sitemap.

Сигнатура `get_car_details` не менялась, вызовы приложения не ломаются.
До применения миграции сайт работает как раньше: снятые объявления
отдают 404, витрины салонов в sitemap не попадают.

## 7. Аналитика (Plausible)

Скрипт подключается только при заданной переменной: пока её нет,
аналитики нет вовсе.

- [ ] Аккаунт Plausible, сайт `rsauto.rs` заведён.
- [ ] `NEXT_PUBLIC_PLAUSIBLE_DOMAIN=rsauto.rs` в Vercel (Production)
      → redeploy.
- [ ] Цели (goals) в дашборде Plausible — все шесть событий уже
      отправляются кодом:

      | Событие | Что означает |
      |---|---|
      | `sell_start` | открыта форма подачи |
      | `listing_submitted` | объявление создано — главная цель |
      | `contact_submitted` | обращение через форму |
      | `seller_contact_click` | клик «Написать продавцу» (в т.ч. гость) |
      | `chat_started` | диалог создан |
      | `login_success` | вход выполнен |

Cookie-баннер не требуется: Plausible не ставит cookie и не собирает
персональные данные.

## 8. Deep links (AASA / assetlinks)

Значения известны только владельцу, из репозитория их взять негде.

- [ ] `TEAM_ID` в `public/.well-known/apple-app-site-association` —
      Apple Developer Portal → Membership → Team ID (10 символов).
- [ ] `SHA256_FINGERPRINT` в `public/.well-known/assetlinks.json` —
      отпечаток **релизного** ключа:
      `keytool -list -v -keystore <release.keystore> -alias <alias>`
      (строка `SHA256:`, вместе с двоеточиями). Debug-ключ не подходит.

До заполнения ссылки открываются в браузере, переход в приложение не
работает. На работу сайта не влияет.

## 9. Публикация

- [ ] `vercel.json` → `git.deploymentEnabled: true` (сейчас `false` —
      автодеплой по push выключен). Включать **в момент релиза**.
- [ ] Прогнать **Lighthouse по боевому URL** после деплоя: главная,
      каталог, карточка объявления. Локально не показательно —
      dev-сборка медленнее продакшена в разы.
- [ ] Проверить `robots.txt` и `sitemap.xml` на боевом домене:
      адреса должны быть на `rsauto.rs`, а не на `*.vercel.app`.

## 10. Приложение

- [ ] **Привести приложение к новому поведению `get_car_details`
      после 0072.** Для `archived` / `rejected` / `moderation` RPC
      теперь возвращает строку со статусом вместо пустого списка.
      Экран карточки в аппе покажет пустые поля — обработать статусы
      явно (своя плашка «объявление снято» в тонах сайта).
      Задача апп-репо, после релиза сайта.

## 11. Отложено намеренно

Не блокирует релиз, зафиксировано, чтобы не потерялось:

- Живые проверки кабинета и сквозного сценария (подача → модерация →
  письмо → чат) на реальном устройстве.
- Дизайн-полировка по итогам живого просмотра.
- Sitemap-index: нужен при росте свыше 50 000 URL, сейчас один файл.
- Интерфейс разбора заявок дилеров (`dealer_leads` читается SQL).
- Оплата промо с сайта (работает только в приложении).
- Поиск кириллицей по латинским названиям («БМВ» не находит «BMW») —
  затрагивает и приложение, требует отдельной задачи.
