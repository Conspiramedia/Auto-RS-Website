// ============================================================
// RS AUTO — Дефолтная OG-картинка русского зеркала (/ru).
// ============================================================
// ЗАЧЕМ ОТДЕЛЬНЫЙ ФАЙЛ, А НЕ РЕЭКСПОРТ. Соседний /ru/car/[id] именно
// реэкспортирует сербский роут — там на картинке только марка,
// модель, город и цена, одинаковые в обеих локалях. Здесь иначе: в
// превью стоит слоган площадки, и он обязан быть на языке того
// зеркала, с которого ссылку переслали.
//
// Человек, отправляющий ссылку со страницы /ru, почти наверняка пишет
// русскоязычному собеседнику — язык адреса и есть сигнал о языке
// получателя. Показывать ему сербский текст значит терять смысл
// превью ровно в тот момент, когда оно работает.
//
// Разметка, фон и композиция общие с сербской версией
// (lib/ogCover.tsx) — правятся один раз для обеих.
//
// Без этого файла страницы /ru отдавали бы сербское превью: файловый
// роут opengraph-image наследуется вниз по дереву, и корневой
// подставился бы и сюда.
// ============================================================

import { renderOgCover, OG_SIZE } from '@/lib/ogCover';

import { brand } from '@/lib/brand';

export const size = OG_SIZE;
export const contentType = 'image/png';
export const alt = `${brand.name} — автомобили в Сербии`;

export default function Image() {
  return renderOgCover('ru');
}
