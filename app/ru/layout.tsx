// ============================================================
// RS AUTO — Layout русской версии сайта.
// ============================================================
// Корневой <html lang> может быть только один на приложение (он объявлен
// в app/layout.tsx как sr-Latn, потому что сербская версия живёт в корне
// сайта). Для поддерева /ru проставляем язык на обёртке: браузер и
// скринридер берут ближайший атрибут lang вверх по дереву, поэтому
// содержимое здесь корректно читается как русское.
//
// Для поисковых систем язык версии определяется не этим атрибутом, а
// взаимными тегами hreflang и canonical — они собираются в lib/seo.ts
// и подставляются каждой страницей.
// ============================================================

export default function RuLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <div lang="ru">{children}</div>;
}
